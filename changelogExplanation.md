# Code Change Log

_This file is auto-maintained by Claude Code. Each entry represents a batch of changes from a session._

---

## [2026-06-04 22:15] — UX overhaul: landing page, phase-based layout, ChatGPT-style sidebar, Stop button, landmark pin, route lines, richer map popups, commute RCA fix

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/instructions.py` | Updated ANALYST_PROMPT: added `photos` field to JSON template; changed STEP 2 from a prose commute summary to embedding the raw `check_commutes` JSON verbatim | Commute times were never appearing on cards — the frontend parser expected the raw Google Maps `"rows"` JSON, but the Analyst was writing text like "25 min commute (15 miles)" instead. Requiring the raw JSON fixes the data mismatch. The photos field tells the LLM to carry photo URLs through the dossier. |
| `apartment_finder/tools.py` | `store_requirements`: added landmark geocoding using the existing `googlemaps` client; stores `landmark_lat/lng/name` in session state. `_fetch_apify`: extracts `imgSrc`/`carouselPhotos` photo URL. `_normalize`: passes `photos` array through. | The map needed a pin for the user's commute destination but only had its name as a string. Geocoding it at the moment the user's requirements are saved means coords are available by the time the frontend draws the map. Photos are optional — they only appear when Zillow's Apify scraper returns an image URL. |
| `api/session_manager.py` | Added `_cancel_flags` dict; `cancel_session()` function; cancel check at top of each SSE loop iteration; expanded state event to include `user_requirements`, `landmark_lat`, `landmark_lng`, `landmark_name` | Without cancel support, clicking "Stop" in the UI would close the browser connection but leave the agent pipeline burning Gemini quota in the background. The cancel flag lets the backend generator detect and exit. The extra state fields let the frontend generate a human-readable session title and place the landmark pin — previously that data existed in session state but was never sent to the frontend. |
| `api/server.py` | Added `POST /api/chat/{session_id}/cancel` endpoint; added `GET /api/directions` endpoint that calls Google Maps Directions API, decodes the polyline, and returns `[[lat, lng], ...]` | Two new capabilities the frontend needed: cancelling a running agent run, and fetching road-following route geometry between an apartment and the landmark. Using the same `googlemaps` client already in the project keeps the API-key surface minimal. |
| `requirements.txt` | Added `polyline` package | Google Maps returns encoded polylines (a compact string format). The `polyline` library decodes them back to lat/lng arrays that Leaflet can draw. Without it, the directions endpoint would need to implement the decoding algorithm manually. |
| `frontend/lib/types.ts` | Added `LandmarkInfo` interface; added `photos?: string[]` to `Apartment`; expanded SSEEvent `state` discriminant to include `user_requirements`, `landmark_lat/lng/name` | Adding new data flowing from the backend needs to be typed end-to-end. The `LandmarkInfo` type is reused by both `useChat` and `MapView` so the shape is defined once. |
| `frontend/lib/parseApartments.ts` | Replaced non-greedy regex `\[[\s\S]*?\]` with a bracket-counting parser (`findTopLevelJsonBlocks`) that respects string literals and nesting; applied same fix to commute JSON extraction; added text-format commute fallback | Adding `"photos": []` to the apartment JSON created a nested `]` inside the array. The old non-greedy regex stopped at the first `]` it found — which was inside `photos: []` — so it never captured the complete apartment array. Result: `apartments` was always empty and the results panel never appeared. The bracket-counter handles arbitrary nesting correctly. The text-format fallback handles cases where the LLM doesn't copy the raw JSON faithfully. |
| `frontend/lib/api.ts` | Added optional `signal?: AbortSignal` parameter to `chatStream`; forwards it to the internal `AbortController` | The stop button needed a way to cancel the HTTP stream. `chatStream` already had its own internal abort controller but no way to trigger it from outside. Forwarding an external signal means `useChat` can abort with a single `abortController.abort()` call. |
| `frontend/hooks/useChat.ts` | Added `AbortController` ref; `stopStreaming()` (aborts stream + calls cancel endpoint); `landmark` state; session title generation from `user_requirements`; landmark persistence in localStorage; landmark restoration in `restoreSession` | Centralises all the new functionality that flows from a completed search: knowing when and how to stop, what the commute destination is, and what to label the session in the sidebar. |
| `frontend/components/AppShell.tsx` | Replaced fixed 38/62 two-column layout with a phase-based system: `landing` (full-width centered), `chatting` (full-width chat, no map), `results` (1/3 chat + 2/3 map with CSS transition); sidebar moved from a floating overlay to a persistent flex-row member; removed hamburger button | The old layout always showed both columns even before any search, making the empty right panel feel like broken UI. The phase system means users see a focused landing page first, then a smooth animated transition to results — matching the mental model of "search then see". |
| `frontend/components/chat/ChatPanel.tsx` | Added `phase` prop; landing phase renders large centered title + roomy textarea + suggestion chips; chatting/results phase renders the existing layout; Send button replaced by red Stop square icon when `isStreaming` | The landing UI matches the Claude/ChatGPT pattern users already know: big text field in the middle of the page. The Stop button replaces the send button (not sits alongside it) to avoid confusing users about which to click during a run. |
| `frontend/components/sidebar/ConversationSidebar.tsx` | Rewrote from a sliding drawer overlay to a persistent dark sidebar panel (240px open, 48px collapsed) that is part of the main layout flex row; changed prop from `onClose` to `onToggle`; loads sessions on mount and polls every 3s to catch title updates from the same tab; cleans up `apt_landmark_*` localStorage on delete | Persistent sidebars (like ChatGPT, Claude) are faster to use because they don't require opening a drawer first. The dark background distinguishes it from the main content area. Polling bridges the gap that `storage` events don't fire for writes in the same browser tab — needed so session titles update live after a search completes. |
| `frontend/components/map/MapView.tsx` | Added amber star landmark pin; rank-colored apartment pins (`makeAptIcon` by index); route polyline fetched on popup open (dashed placeholder while loading, road-following polyline on success); enhanced popup with photo, rank badge, commute info | The map previously showed only price and address on click. The redesign makes each pin self-contained: users can see the ranking, commute time, and a photo without switching to the cards tab. The route line gives a spatial sense of the commute that a duration number alone doesn't convey. Routes are fetched lazily (only when a pin is opened) to avoid burning Directions API quota for all apartments upfront. |
| `frontend/components/results/ResultsPanel.tsx` | Added `landmark?: LandmarkInfo` prop; passes it through to `MapView`; changed default tab from `"cards"` to `"map"` | The map is now the richer view (landmark pin, route lines, photo popups), so it makes sense to show it first. Landmark needs to be threaded from `AppShell` → `ResultsPanel` → `MapView`. |

### Decisions & Assumptions
- **Bracket-counting parser vs. stricter JSON schema in instructions**: Tightening the LLM prompt to output only what the parser expects is brittle — models drift. The bracket-counter is defensive infrastructure that tolerates any valid JSON the LLM produces, however nested.
- **Route polylines are fetched lazily on popup open**: Fetching routes for all apartments upfront would cost one Directions API call per listing every time results load. Lazy fetching means the user only pays for routes they actually look at.
- **Straight dashed line shown as placeholder while route loads**: Gives immediate visual feedback that the routing is in progress, rather than showing nothing until the fetch completes.
- **Session titles polled every 3s (same-tab limitation)**: The `storage` event only fires in *other* tabs. Since `useChat` writes the session title in the same tab, the sidebar would never see the update via events alone. 3s polling is a lightweight workaround — not ideal but invisible to the user.
- **Stop cancels both frontend stream and backend pipeline**: Cancelling only the frontend SSE connection would leave the ADK agent runner consuming Gemini quota in the background with no way to retrieve results. The backend cancel flag exits the generator cleanly.
- **`polyline` package chosen over manual decode**: Google's polyline encoding algorithm is well-specified but ~20 lines of bit-manipulation code. The `polyline` PyPI package is battle-tested and adds negligible weight to the environment.

---

## [2026-06-04 18:32] — Reduce Gemini rate limits via model split; fix 5 UI/pipeline bugs

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/agent.py` | Replaced single shared `model = Gemini(...)` with `gemini_model` (Reviewer only) and `openai_model` (Manager, Analyst, Summarizer) using ADK's `LiteLlm` wrapper | The Gemini 2.5 Flash free tier allows very few requests per minute. Running all 4 agents through it means every search burns 4+ quota slots. Moving 3 agents to OpenAI (gpt-4o-mini) cuts Gemini usage by ~75%. The Reviewer must stay on Gemini because ADK's built-in `google_search` grounding tool is Gemini-native and silently fails on other providers. |
| `requirements.txt` | Added `litellm` | ADK's `LiteLlm` wrapper delegates to the `litellm` package under the hood. Without it installed, importing `LiteLlm` throws `ModuleNotFoundError` at startup. |
| `apartment_finder/tools.py` | Changed `fetch_apartments` provider loop: empty `[]` results now `continue` to next provider instead of returning "no results" immediately | RentCast was returning an empty list for expensive markets like Seattle (no listings under budget) rather than raising an error. The old code treated empty-but-successful as final, never trying Apify or the local CSV. Now empty results are treated like a soft miss — the chain keeps trying until all providers are exhausted. |
| `api/session_manager.py` | Added tool-call-based status event detection in `stream_message` — inspects `part.function_call.name` to infer which sub-agent is active | ADK's `event.author` field doesn't reliably carry sub-agent names for agents nested inside a `SequentialAgent`. The pipeline status bar (Analyst, Reviewer steps) was never lighting up. Detecting `fetch_apartments`/`check_commutes` calls → Analyst active, and `google_search` calls → Reviewer active gives reliable status signals regardless of how ADK sets `author`. |
| `frontend/components/results/ResultsPanel.tsx` | Changed `next/dynamic` import for `MapView` to wrap the named export as `{ default: m.MapView }` | `next/dynamic` expects a module with a `default` export. Named exports (`export function MapView`) don't satisfy that contract — React's lazy loader called `render` on the module object itself, which is not a function. Wrapping it as `{ default: m.MapView }` makes it look like a default export to `dynamic`. |
| `frontend/hooks/useChat.ts` | Added `currentBubbleId`/`currentBubbleAuthor` tracking so each agent author gets its own message bubble; replaced hardcoded `assistantMsgId` references | Manager and Summarizer both emit non-intermediate tokens, but they were all appended to the same placeholder bubble — the user saw one concatenated block of text with no visible handoff. Now when the token author changes, a new empty bubble is created and subsequent tokens go into it, giving each agent its own distinct chat message. |
| `frontend/hooks/useChat.ts` | Added `useEffect` to persist `apartments` to `localStorage` under `apt_apartments_<sessionId>`; `restoreSession` now reads it back instead of always calling `setApartments([])` | When revisiting a past session from the sidebar, the chat messages were restored but the result cards and map were blank. Apartments are now stored alongside messages and reloaded on session restore. Only sessions created after this change will have persisted apartments. |
| `frontend/package.json` + `package-lock.json` | Downgraded `react-leaflet` from `^5.0.0` to `^4.2.1`; `@react-leaflet/core` also downgraded from `3.0.0` to `2.1.0` | `react-leaflet@5` dropped React 18 support and requires React 19. The project uses React 18.3.1. When the Map tab was clicked, React's `updateContextConsumer` called `render` on an object that isn't a function — because v5 uses React 19's new context API internally. v4.2.1 is the latest React 18-compatible release and has the same component API. |

### Decisions & Assumptions
- **`gemini-2.5-flash-lite` chosen over `gemini-2.5-flash` for the Reviewer**: Only 1 of 4 agents remains on Gemini, so the lighter model reduces free-tier pressure further. The Reviewer only does web search + summarization — it doesn't need the full Flash model's reasoning depth.
- **`gemini-2.0-flash` was ruled out**: Deprecated and shut down June 1, 2026. Cannot be used.
- **Tool-call detection uses `seen_authors` guard**: Both the author-based and tool-call-based status triggers write to `seen_authors`, so if ADK *does* surface `author="analyst"` on some events, duplicate status events are suppressed.
- **Apartment persistence is forward-only**: Sessions completed before this commit have no `apt_apartments_*` entry in localStorage. Restoring them will show messages but not result cards — this is expected and not a regression.

---

## [2026-06-01 16:16] — Build React UI: Next.js frontend + FastAPI backend bridge, fix 429 crash and agent status visibility

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `api/__init__.py` | New file (empty package marker) | Makes `api/` a proper Python package so `api.server` and `api.session_manager` can be imported. |
| `api/session_manager.py` | New file — wraps ADK `InMemoryRunner`, manages one session per user conversation, streams SSE events (status, token, state, done, error) | The CLI's `main.py` kept one runner alive for one user. For a multi-user web app each browser tab needs its own isolated runner+session. This module owns that registry in a dict. Each agent author name (`analyst`, `reviewer`, etc.) is mapped to a human label emitted as a `status` event so the frontend can show which pipeline step is running. `runner.run_async()` is wrapped in `try/except` so a 429 rate-limit error yields a friendly `error` SSE event instead of crashing the ASGI process. |
| `api/server.py` | New file — FastAPI app with CORS, three REST endpoints + SSE streaming | The ADK runner is Python-only; the React frontend needs an HTTP interface. FastAPI's `StreamingResponse` with `text/event-stream` media type lets the browser receive real-time agent events as they happen, rather than waiting for the full response. Three endpoints: `POST /api/sessions` (create session), `POST /api/chat/{id}` (stream events), `GET /api/sessions/{id}/state` (fetch session state for card hydration). |
| `requirements.txt` | Added `fastapi` and `uvicorn[standard]` | Runtime dependencies for the new API server. |
| `package.json` | Added `dev`, `api`, `ui` scripts; added `concurrently` devDependency | Lets developers start both servers with a single `npm run dev` command from the repo root instead of managing two terminal windows manually. Uses `127.0.0.1` explicitly in the uvicorn command to avoid the Windows IPv6 (`::1`) resolution mismatch. |
| `frontend/` | New directory — entire Next.js 14 app (App Router, TypeScript, Tailwind CSS v3) | The CLI-only interface needed a visual product that any user could open in a browser. Next.js 14 (not 16) was used because Node v18 is installed on this machine — Next.js 16 requires Node ≥ 20 and its `@tailwindcss/oxide` binary would fail to install. |
| `frontend/next.config.mjs` | New file — proxies `/api/*` to `http://127.0.0.1:8000` | Next.js dev server and FastAPI run on different ports. A proxy rewrite lets the frontend call `/api/...` without CORS headers. `localhost` was replaced with `127.0.0.1` because Windows Node resolves `localhost` to IPv6 `::1`, while uvicorn binds to IPv4, causing `ECONNREFUSED`. |
| `frontend/tailwind.config.ts` | New file — Tailwind v3 config with custom design tokens | Defines the app's color palette (primary blue `#1A56DB`, accent sky `#0EA5E9`, surface whites, muted gray) and custom keyframe animations (`fadeIn`, `slideUp`, `pulseDot`) used by the agent status dots. |
| `frontend/app/globals.css` | New file — Tailwind directives, Google Fonts Inter, leaflet CSS import, scrollbar styling | Tailwind v4 uses `@import "tailwindcss"` but v3 needs `@tailwind base/components/utilities`. Leaflet's CSS must be imported globally (not scoped) or the map tiles render without positioning. |
| `frontend/app/layout.tsx` | Replaced scaffold — sets page title/description, clean `h-full` body | The scaffold imported Geist fonts (unavailable in Next.js 14) and added dark-mode variables incompatible with our design system. Simplified to just set metadata and pass through children. |
| `frontend/app/page.tsx` | Replaced scaffold — renders `<AppShell />` | Single-entry point; all logic lives in components. |
| `frontend/components/AppShell.tsx` | New file — 38/62 split layout, wires `useChat` hook to panels | Two-column CSS grid: left 38% for chat, right 62% for results. Owns the sidebar open/close toggle. Passes chat state (messages, apartments, agentStatus) down to both panels so they stay in sync. |
| `frontend/components/chat/ChatPanel.tsx` | New file — message list, suggestion chips, streaming textarea input | The `AgentStatus` component is placed **outside** the scrollable messages div, between messages and the input bar. This means the pipeline tracker is always visible while the agent works, regardless of how far the user has scrolled. |
| `frontend/components/chat/MessageBubble.tsx` | New file — renders user (right-aligned blue) and assistant (left-aligned, with avatar icon) messages | Distinguishes user and AI messages visually so the conversation is scannable at a glance. Empty assistant messages are hidden to avoid a blank bubble flash at stream start. |
| `frontend/components/chat/AgentStatus.tsx` | New file (then redesigned) — 4-step pipeline tracker (Requirements → Listings → Safety → Summary) | The original design was a small pill buried in the scroll area — easy to miss. The redesign shows a permanent step-by-step pipeline bar with the active step highlighted in blue, completed steps shown with a green checkmark, and animated dots next to the current step label. Because it lives outside the scroll container, it's always on screen while the agent is working. |
| `frontend/components/results/ResultsPanel.tsx` | New file — tabs (Cards / Map), empty state with 3-step explainer | Conditionally shows apartment cards or the Leaflet map based on the selected tab. Uses `next/dynamic` with `ssr: false` to load the map only in the browser — Leaflet uses `window` which doesn't exist during server-side rendering. |
| `frontend/components/results/ApartmentCard.tsx` | New file — price, address, commute badge (color-coded), safety note, stale-data warning | Ranks apartments with labels ("Top Pick", "Runner-up", "Budget Pick") and colour codes the commute badge green/amber/red based on drive time. Cards update the map highlight on hover via the shared `hoveredId` state. |
| `frontend/components/results/CommuteBadge.tsx` | New file — shows drive time + distance with colour coding | Green ≤ 20 min, amber 21–40 min, red > 40 min. Makes commute quality immediately readable without needing to read numbers. |
| `frontend/components/map/MapView.tsx` | New file — react-leaflet map with custom div icons, FitBounds effect | SSR-disabled via dynamic import. Fixes Leaflet's broken default icon path (webpack strips the `_getIconUrl` method) by manually setting CDN URLs. Custom `divIcon` markers instead of PNG images so highlighted state can be applied with CSS without loading extra assets. `FitBounds` component auto-zooms to fit all apartment pins on mount. |
| `frontend/components/sidebar/ConversationSidebar.tsx` | New file — slide-in drawer with session history from localStorage | Each search is stored in `localStorage` by `useChat` when the session is created. The sidebar reads this on open and shows the first 60 characters of the first message as the session title, plus a relative timestamp. |
| `frontend/hooks/useChat.ts` | New file — SSE stream parser, session lifecycle, apartment state | Uses `fetch` + `ReadableStream` (not `EventSource`) for full control over the connection. Parses `data: {...}` SSE lines: `status` events update the pipeline tracker, `token` events append to the assistant message (only from `manager` or `summarizer` authors — intermediate agents' tokens are filtered out), `state` events trigger apartment card parsing, `error` events replace the assistant placeholder with a human-readable error message, `done` clears the status bar. |
| `frontend/lib/types.ts` | New file — shared TypeScript interfaces | Single source of truth for `Apartment`, `CommuteInfo`, `ChatMessage`, `AgentStatusEvent`, and the `SSEEvent` discriminated union. |
| `frontend/lib/api.ts` | New file — typed fetch wrappers for FastAPI endpoints | Abstracts session creation and SSE stream setup so hooks don't contain raw fetch logic. |
| `frontend/lib/parseApartments.ts` | New file — extracts apartment data from ADK session state | The Analyst agent writes a prose + JSON dossier to session state. This parser uses regex to find embedded JSON arrays in the text, validates that they have `latitude`/`longitude` fields, and maps them to typed `Apartment` objects. Also parses Google Maps Distance Matrix responses to attach commute info to each card. |

### Decisions & Assumptions
- **Next.js 14 instead of 16**: Node v18 is installed on this machine; Next.js 16 requires Node ≥ 20. Tailwind CSS v4's `@tailwindcss/oxide` binary also requires Node 20 and will fail to compile. All Tailwind v4-specific syntax (`@import "tailwindcss"`, `@theme`) was replaced with v3 equivalents.
- **`127.0.0.1` instead of `localhost` in proxy and uvicorn**: Windows resolves `localhost` to `::1` (IPv6) in Node.js but uvicorn binds to `0.0.0.0` / `127.0.0.1` (IPv4) by default, causing `ECONNREFUSED` on the first API call. Hardcoding the IPv4 address in both the Next.js proxy config and the uvicorn startup command eliminates the ambiguity.
- **Agent status moved outside scroll area**: The pipeline tracker was originally inside the scrollable messages div — it appeared at the bottom of the message list and was invisible if the user had scrolled or if the chat was short. Moving it to a fixed slot between the message list and the input bar means it's always on screen while the agent is working.
- **Only `manager` and `summarizer` tokens are rendered**: The Analyst, Reviewer, and Research Team agents produce internal reasoning text (tool call results, JSON dossiers) that isn't meant for the user. Filtering by author name ensures only the final Manager Q&A responses and Summarizer recommendation text appear in the chat.
- **429 error is caught in `session_manager.py`, not `server.py`**: The exception originates inside the `runner.run_async()` async generator. If caught at the FastAPI layer the generator is already broken; catching it at the source lets us yield a proper `error` SSE event and close the stream cleanly before FastAPI sees it.
- **Session history is localStorage-only**: Full message history restoration from the backend would require storing every message — out of scope for this session. The sidebar shows past sessions for reference and allows starting fresh; replaying a past session resets to a new blank conversation.

---

## [2026-06-01 13:34] — Add API cost guardrails for RentCast and Apify providers

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/tools.py` | Added `_RENTCAST_MONTHLY_LIMIT`, `_RENTCAST_MAX_PER_RUN`, `_RENTCAST_USAGE_FILE` constants; added `_rentcast_run_count` module-level counter; added `_load_rentcast_usage()` and `_save_rentcast_usage()` helpers; updated `_fetch_rentcast()` to enforce both limits before making any HTTP call | RentCast's free tier only allows 50 calls per month. Without a guardrail, a handful of test runs could exhaust the whole month's quota silently. The per-run cap (2 calls) prevents a single runaway session from burning most of the budget, while the monthly cap (50) automatically reroutes to Apify the moment that ceiling is hit — no manual monitoring needed. Usage is persisted to `data/rentcast_usage.json` so the count survives process restarts and resets itself automatically when the calendar month rolls over. |
| `apartment_finder/tools.py` | Added `_APIFY_MAX_BUDGET_USD`, `_APIFY_COST_PER_1000`, `_APIFY_MAX_ITEMS` constants; replaced hardcoded `maxItems: 5` with `safe_max_items = min(5, _APIFY_MAX_ITEMS)` in `_fetch_apify()` | The Zillow scraper actor costs $2.30 per 1,000 results. At 5 results per call this is cheap, but if `maxItems` is ever raised during development the cost could spike. The guardrail derives the maximum safe item count from the $0.20/run budget (`0.20 / 2.3 * 1000 = 86`) and enforces it at the call site, so future changes to the requested count can never accidentally exceed the budget. |
| `apartment_finder/tools.py` | Added `from datetime import datetime` import | Required by `_load_rentcast_usage()` to stamp and compare the current calendar month. |

### Decisions & Assumptions
- **Usage counter increments after a successful HTTP response**, not before. This means a failed or timed-out request doesn't consume a quota slot — only actual successful API calls are counted against the monthly limit.
- **Monthly reset is implicit**: `_load_rentcast_usage()` checks the stored month string against the current month. If they differ (new month), it returns a fresh zero-count object. No cron job or manual reset is needed.
- **Apify `maxItems` is currently 5**, which is already far below the 86-item guardrail ceiling. The guardrail is latent protection — it activates only if the requested count is ever raised.
- **Per-run RentCast counter (`_rentcast_run_count`) lives in module memory** and resets when the process restarts. This maps naturally to "one agent session = one run" since `main.py` starts a fresh process each time.

---
