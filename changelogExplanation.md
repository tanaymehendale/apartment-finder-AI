# Code Change Log

_This file is auto-maintained by Claude Code. Each entry represents a batch of changes from a session._

---

## [2026-07-14 00:45] — Phase 3 action buttons; deterministic status-routing + per-person-rent fixes (FU-3..FU-6)

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `frontend/components/results/ApartmentCard.tsx` | Added "View listing" and "Directions in Google Maps" action buttons (P3-1/P3-2). Listing button label switches by `listing_source` ("View on Zillow" vs "Search listings"); Directions button builds a client-side `google.com/maps/dir` URL from the listing's coords and the landmark's coords, `travelmode` hardcoded to "driving". Both open in a new tab. | Users could see a listing on the map/card but had no way to click through to the actual rental listing or get turn-by-turn directions — this makes the results actionable instead of read-only. |
| `frontend/components/map/MapView.tsx` | Same two buttons added to the Leaflet marker popup (`AptPopup`), so the map view has the same actions as the card view. | Users browsing via the map tab (not the card list) had no way to act on a pin — parity with the card view. |
| `frontend/components/results/ResultsPanel.tsx` | Threads the `landmark` prop down to `ApartmentCard` (was already available for `MapView`). | `ApartmentCard` needs the landmark's coordinates to build its new Directions button URL. |
| `apartment_finder/tools.py` | **(FU-3/FU-4)** `fetch_apartments` now takes `tool_context` and writes the TRUE search outcome (`search_status`: "ok"/"no_match_in_budget"/"no_results", + `suggested_budget`) into session state in code, the moment it's computed — not left to the Reviewer/Summarizer to infer from prose 3 hops later. Provider chain no longer stops at the first provider with ANY non-empty pool; it now merges pools (new `_dedupe_pool`) and only stops once `_MIN_IN_BUDGET_TARGET=3` in-budget listings are found, so a thin RentCast pool falls through to Apify. Added `build_fallback_recommendation`/`extract_leading_json_array` — a deterministic, code-only way to rebuild a correct reply from state if the LLM chain ever produces a false "no results" bail-out. **(FU-5)** New `_attach_per_person_price(listings, roommates)` computes `per_person_price` AND a fully-formatted `per_person_label` ("$X per person, split N ways") once in Python, attached to every returned listing — removes per-person arithmetic AND wording from the LLM's responsibility entirely. | A user reported getting told "no apartments found" in the chat while the map/cards correctly showed 5 real listings — the pipeline's "did we find anything" signal was carried purely as free-text markers across 3 LLM hops, with a loose substring check that could misfire. Making the outcome a code-computed state key removes the guesswork. Separately, RentCast's free-tier pool can be too sparse to reflect the real market, so a single successful-but-thin provider call was silently preventing Apify (broader Zillow coverage) from ever being tried. On the per-person side, the LLM was literally dividing an already-correct per-person number by 2 again in its reply text (and, after a first fix, copying a hardcoded "2 ways" from a prompt example regardless of the real headcount) — moving both the number and the exact wording into code eliminates that whole class of arithmetic/formatting mistake. |
| `apartment_finder/agent.py` | **(FU-3)** Added `_reviewer_before_callback` and `_summarizer_before_callback`, wired via `before_agent_callback` on the `reviewer`/`summarizer` `LlmAgent`s. Reviewer's callback skips the Gemini/`google_search` call entirely (saves a call) when `search_status` is a code-verified true "no_results". Summarizer's callback is a safety net: if `search_status` says real listings exist but `safety_report` still looks like the erroneous bail-out, it skips the LLM call and returns `tools.build_fallback_recommendation`'s output instead. | This is what actually prevents the false "no results" message from ever reaching the user — the gate runs *before* the agent's own (unreliable) text generation, using the ADK `before_agent_callback` API (confirmed against the installed `google-adk==1.19.0`) rather than hoping the LLM notices a marker string correctly. |
| `apartment_finder/instructions.py` | **(FU-3)** `ANALYST_PROMPT`'s JSON schema gained `per_person_price`/`per_person_label` fields with "copy exactly, never recompute" instructions; passes `roommates` through to `fetch_apartments`. **(FU-5)** `SUMMARIZER_PROMPT`'s roommate-economics section rewritten to copy the given `per_person_label` verbatim instead of computing or reformatting any part of it, with the two prior failure modes spelled out as negative examples. **(FU-6)** `MANAGER_PROMPT` gained a new "ROOMMATES / OCCUPANT COUNT — BE STRICT, NEVER GUESS A NUMBER" section: defines "roommates" as people other than the user, forbids inferring a headcount from budget phrasing like "$1800 per person" alone, and requires the Manager to ask a clarifying question ("How many people total will be splitting the rent, including you?") when per-person phrasing appears with no stated count. | The Analyst/Summarizer changes are the prompt side of the FU-3/FU-5 code fixes above — the LLM needs to be told to relay the code-computed values, not recompute them. The Manager change fixes a third, different bug found live: the user typed "...under 1800 per person" with no headcount at all, and the Manager fabricated `roommates=2` anyway (confirmed by the per-person math dividing exactly by 3). Unlike the other two, occupant count can only come from the user's own words — there's no code-level value to fall back to — so the fix is a mandatory clarifying question instead of a silent guess or a silent solo default (either of which would search at a budget the user didn't intend). |
| `test_tools.py` | Added 9 new offline tests: `test_provider_merge_supplements_thin_pool`, `test_provider_merge_skips_second_when_sufficient`, `test_search_status_recorded_deterministically`, `test_per_person_price_single_division`, `test_per_person_label_matches_actual_occupant_count`, `test_build_fallback_recommendation_corrects_false_negative`, `test_build_fallback_recommendation_noop_when_consistent`, `test_reviewer_callback_skips_on_true_no_results`, `test_summarizer_callback_overrides_false_negative`. Total: 24 offline tests, all passing. | Every code-computed behavior above (provider merge trigger, `search_status` recording, per-person math + label, the two `before_agent_callback` guards) is a pure function or a callback with a fakeable context, so it's fully testable without hitting a real LLM or burning API quota — these tests are what let the fixes be verified with confidence in this session (the Manager clarifying-question behavior in FU-6 is the one exception: it's a live LLM judgment call, not offline-testable, and is flagged for a live spot-check instead). |

### Decisions & Assumptions
- **Deterministic code gates over prompt tightening, per explicit user preference** — when a decision is already fully computable from structured data (search outcome, per-person arithmetic), compute it once in Python and gate the LLM agents on that value via `before_agent_callback`, rather than trusting the LLM to relay/recompute it correctly across multiple hops. This is now the established pattern for this repo's agent work.
- **Not every bug in this family is code-fixable** — FU-6 (occupant count) is inherently free-text extraction with no backing computation; the fix there is a mandatory clarifying question in the Manager's prompt, not a code gate. Distinguishing "LLM redundantly recomputing a code-known value" from "LLM extracting a fact only the user knows" determined which fix pattern applied.
- **No `roommates`→`occupants` rename** — the user pointed out "roommates" is ambiguous (does it include the user?) and suggested "occupants" would be clearer. Addressed by adding an explicit definition inline in `MANAGER_PROMPT` rather than renaming the field across the ~10+ files that reference it (backend schema, prompts, frontend types/components, localStorage keys) — that's a bigger refactor with its own regression risk on an already-verified feature (P2-3), left as an open option pending the user's decision.
- **`travelmode` hardcoded to "driving"** for the new Directions button — the chosen commute mode isn't currently threaded from `check_commutes(mode=...)` through to the frontend (`CommuteInfo` has no `mode` field), and "driving" matches the tool's own default. Deferred rather than adding new plumbing, since P3-2's scope was explicitly "pure client-side, no backend."
- **Langfuse observability scheduled before Phase 4** (new "Phase 3.5" in the plan file) — diagnosing the FU-3 bug required manually reasoning through prompt text with zero visibility into what the Reviewer/Summarizer actually received/produced at each hop; a trace tool would have shown this directly. Chosen over Google Cloud Trace/Arize Phoenix/LangSmith because `google-adk` already depends on the OpenTelemetry/OTLP stack and the Analyst/Summarizer already run through LiteLLM, both making Langfuse integration near-zero-lift. Needs the user to create an account/API keys first — not provisioned unattended.

---

## [2026-06-24 00:06] — Phase 2: roommate economics, proximity amenities + transit, RequirementCards UI, 503 mitigation, SSE streaming fix

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/tools.py` | Added `find_nearby_amenities(origins, label, kind)` tool with three kind branches: `named` (geocode + haversine), `category` (Places New Text Search + rankPreference=DISTANCE per listing), `transit` (Places New searchNearby typed by `_transit_type_for`, per listing). Added `_PLACES_MAX_PER_RUN=25` + monthly `data/places_usage.json` guardrail + in-proc `_PLACES_CACHE` keyed on rounded coords|label. Added `_transit_type_for` mapping free text → Places station type (light_rail checked before rail). `_compute_requirements` now defaults `min_bedrooms = roommates + 1` when roommates > 0 and beds unset. `store_requirements` folds in `pending_optional` from session state (merges optional prefs seeded by cards, explicit LLM-passed values win). | The old `kind:"named"` geocode for "Caltrain" returned the city ZIP centroid, not an actual station. `searchNearby` with typed station lookups returns the nearest real station per listing. The guardrail prevents Places API cost spikes during heavy testing. The roommates→bedrooms default ensures a solo user with 2 roommates doesn't see studios. The `pending_optional` fold-in lets the RequirementCards UI pass a nested `proximity` list to `store_requirements` deterministically — the LLM can't pass structured lists through tool arguments. |
| `apartment_finder/agent.py` | Added `ResilientGemini(BaseLlm)` wrapper: primary=`gemini-2.5-flash-lite`, fallback=`gemini-2.5-flash`; on transient errors (503/504/UNAVAILABLE/OVERLOADED/INTERNAL/DEADLINE) does 2 short retries (3 s) then falls back to full Flash once; 429 propagates untouched to `exp_base=7` path. Narrowed `retry_config.http_status_codes` from `[429,500,503,504]` to `[429]` only. Registered `find_nearby_amenities` on the Analyst. | Gemini 2.5 Flash-lite shed load with 503 "model overloaded" 6× per session (free-tier capacity issue, not quota). `exp_base=7` applied to 503 produced multi-minute retry waits. Narrowing to 429-only removes that; `ResilientGemini` handles transient 503 with a short retry then falls back to full Flash, restoring results without the long wait. 429 still gets the `exp_base=7` exponential backoff since it's genuine quota exhaustion. |
| `apartment_finder/instructions.py` | Manager: added `STRUCTURED PREFERENCES RULE` — parse only 4 required fields + call `store_requirements` when message contains `[STRUCTURED_PREFERENCES]`. Analyst: added STEP 2b (append raw proximity JSON verbatim) and STEP 3 (call `find_nearby_amenities` once per proximity entry). Reviewer: changed from one `google_search` per apartment to **one batched search** covering all neighborhoods. Summarizer: added `{user_requirements}` template var, ROOMMATE & PER-PERSON section (state per-person rent when roommates > 0), PROXIMITY section (factor badge data into ranking). | The batched Reviewer drops ~5× Gemini calls from ~6 to 1, directly cutting 503 exposure. Passing raw proximity JSON verbatim (not through LLM summarization) means the frontend parser can extract structured badge data reliably. The per-person rent section ensures roommate economics are surfaced in the recommendation text, not just on cards. |
| `api/session_manager.py` | Replaced early-return-on-error with `run_error = error_msg; break` pattern. After the loop, always emits the `state` event if `analyst_dossier` or `final_recommendation` exist (result salvage). Added friendly error messages for 503/429/generic failures. P2-5 bridge: when `requirements` payload is optional-only (missing city/state/budget/landmark), seeds `pending_optional` via `append_event(state_delta=...)` and appends `[STRUCTURED_PREFERENCES]` to the message text. | Before this fix, a Reviewer 503 caused `session_manager` to early-return, discarding the Analyst's already-computed listings/commutes/proximity → blank UI. Now partial results always reach the frontend even when downstream agents fail. The two-path bridge is needed because `store_requirements` can't accept a nested `proximity` list via normal LLM tool arguments — the optional-only path pre-seeds it so `store_requirements` folds it in deterministically. |
| `api/server.py` | Added `allow_origin_regex=r"http://(localhost\|127\.0\.0\.1):\d+"` to CORS middleware. | The SSE fix (see `api.ts`) routes the browser directly to the FastAPI backend on port 8000, bypassing Next.js. Without CORS permitting the dev frontend's origin (any localhost port), the browser blocks the cross-origin request. |
| `frontend/lib/api.ts` | Added `STREAM_BASE = process.env.NEXT_PUBLIC_BACKEND_URL ?? ""` constant. Changed `chatStream` fetch URL to `${STREAM_BASE}${BASE}/chat/${sessionId}`. Added `proximity` field to `RequirementsPayload`. | Next.js dev buffers `text/event-stream` responses on BOTH the `rewrites()` proxy AND App Router route handlers — all events arrive in one dump at the end, making the UI look frozen on "Manager" for ~40 s. Setting `NEXT_PUBLIC_BACKEND_URL` routes the SSE fetch directly from the browser to uvicorn, which streams live. Proxy/rewrites are still used for non-streaming API calls in both dev and production. |
| `frontend/.env.local` | New file: `NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000` | Must be set for dev to enable the SSE direct-fetch bypass. File is gitignored; must be created per-dev-environment. |
| `frontend/next.config.mjs` | Added `BACKEND_URL` env var read from `process.env.BACKEND_URL` for the rewrites destination. | Allows the rewrite proxy target to be overridden in CI/staging without changing code. |
| `frontend/lib/types.ts` | Added `ProximityResult` interface (`label`, `name`, `distance_text`). Added `proximity_results?: ProximityResult[]` to `Apartment`. | New data flowing from the backend needs end-to-end types. `ProximityResult` is used by both `parseApartments` and `ApartmentCard`. |
| `frontend/lib/parseApartments.ts` | Added proximity block parsing after the commute block loop. Detects `{"label","kind","results":[...]}` objects in the dossier and attaches `proximity_results` by origin index to each apartment. | The Analyst appends proximity JSON verbatim to the dossier. The parser must extract it and map it back to the right apartment by listing index, since the results are ordered parallel to the origins array. |
| `frontend/hooks/useChat.ts` | Added `roommates` state persisted to `localStorage` (`apt_roommates_<id>`). `sendMessage` accepts optional `requirements?: RequirementsPayload`. Parses `r.roommates` from `event.user_requirements` on state event. Resets/restores `roommates` on `resetSession`/`restoreSession`. | Roommate count is needed by `ApartmentCard` to compute per-person rent, but it comes from session state (set during requirements intake) rather than free-form chat. Persisting it in localStorage ensures the card still shows per-person rent when the user navigates away and returns. |
| `frontend/components/AppShell.tsx` | Threads `roommates` from `useChat` down to `ResultsPanel`. | Prop drilling: `useChat` owns the state, `ApartmentCard` renders it — `AppShell` and `ResultsPanel` are the intermediaries. |
| `frontend/components/results/ResultsPanel.tsx` | Accepts and threads `roommates?: number` to `ApartmentCard`. | See above — prop threading. |
| `frontend/components/results/ApartmentCard.tsx` | Computes `perPerson = Math.round(monthly_price / (roommates + 1))`. Renders `"$X/person · split N ways"` next to price when `roommates > 0`. Renders emerald proximity badges from `apartment.proximity_results`. | Per-person rent makes shared-living economics immediately visible without requiring the user to do mental math. Proximity badges give at-a-glance distance to the nearest station/amenity without cluttering the main commute badge. |
| `frontend/components/chat/RequirementCards.tsx` | New file — collapsible "Add preferences (optional)" panel on the landing page. Quick-near chips (Caltrain → `kind:"transit"`, Bus stop → `kind:"transit"`, Grocery store → `kind:"category"`, etc.). Emits optional-only `RequirementsPayload` via `onChange`; empty = plain free-text path. | The optional preferences (min beds/baths, roommates, proximity) were only reachable by typing them into free text, which the Manager sometimes mis-parsed. Cards give a one-click path that bypasses LLM parsing entirely and seeds them deterministically via the `pending_optional` bridge. |
| `frontend/components/chat/ChatPanel.tsx` | Imports `RequirementCards`; maintains `reqPayload` state via `handleReqChange`; passes `reqPayload` to `onSend` in landing phase only. | Wires the card panel into the send flow. The payload is only forwarded in the landing phase (where the panel is visible); in chatting/results phase `requirements` is undefined so the plain text path is used. |
| `test_tools.py` | Added 7 new offline tests: `test_roommates_default_bedrooms`, `test_proximity_named_offline`, `test_proximity_category_guardrail_offline`, `test_transit_type_mapping_offline`, `test_transit_branch_offline`, `test_pending_optional_foldin`, `test_resilient_gemini_offline`. Total: 12 offline tests. | Guardrail, routing, and fold-in logic are all testable without burning API quota. The `ResilientGemini` test uses a fake LLM to verify fallback triggers on 503 and suppresses non-transient errors. |
| `CLAUDE.md` | Updated: Reviewer description changed to "one batched google_search"; Places API notes updated (searchNearby requires locationRestriction, not locationBias). | Keeps on-disk docs aligned with the implemented system to avoid confusing future Claude sessions. |

### Decisions & Assumptions
- **`exp_base=7` MUST NOT be changed** — confirmed via git history (commit 05d34cf) and user. `exp_base=2` retries inside Gemini's 60s RPM window and fails every time; `exp_base=7` pushes retries (7 → 49 → 343 s) past the window so the quota refills.
- **Tier 1 transit only (no GTFS/Transitland)** — route-level precision (e.g. "nearest Caltrain station specifically on the Caltrain line") was scoped out by the user. Deferred as P2-4-FU in the plan. Google Places typed `searchNearby` gives nearest station by type (train/bus/subway/light_rail), which is correct for most urban transit.
- **P2-2 multi-area search: PARKED** — user parked it due to input-surface ambiguity (RequirementCards would need a multi-city picker and the UX is unclear). The `areas` field already exists in the schema so future work is additive.
- **SSE fix is dev-only bypass, not production change** — `NEXT_PUBLIC_BACKEND_URL` is only set in `frontend/.env.local` (gitignored). Production uses the same-origin route handler (Next.js App Router `frontend/app/api/chat/[sessionId]/route.ts`), which does not buffer in production (only Next dev has this limitation).
- **Result salvage always emits state if Analyst data exists** — even when the Reviewer or Summarizer fails, users see listings + commutes + proximity badges. The error message explains the safety-research step failed and asks them to retry.
- **`pending_optional` seeds via `append_event(state_delta=...)` not direct mutation** — ADK `InMemorySessionService.get_session()` returns a copy; in-place writes are silently lost. State changes must go through `append_event`.

---

## [2026-06-23 01:00] — Phase 1 + Foundation (accuracy & schema), Summarizer-driven ranking, RentCast per-run + Apify schema fixes

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/tools.py` | Removed the local-CSV provider entirely (`_get_local_df`, `_fetch_local_csv`, pandas import). Added `VALID_US_STATES`. Added `_places_text_search` (Google Places API New via httpx) and switched landmark resolution to it with a city-centroid `locationBias` circle. Expanded `_normalize` to carry `bedrooms`/`bathrooms`/`square_feet` + `listing_url`/`listing_source`. Split requirements persistence into a pure `_compute_requirements` (returns a state delta) + `_seed_requirements` wrapper. Rewrote `fetch_apartments` to fetch a **price-uncapped** pool and partition it (in-budget + a ≤15% stretch, or `no_match_in_budget`+`suggested_budget`). Reset the RentCast per-run counter at the start of each call. Rewrote `_fetch_apify` to the actor's real schema (`searchUrls` with a viewport-derived `searchQueryState`, `MAP_MARKERS`, run-level cost guardrail) + `_build_zillow_search_url`/`_parse_price`. | This is the accuracy core. The CSV served stale data, so it's gone — at least one live provider is now required. The old landmark lookup resolved "HCLTech Redmond office" to the city center; Places Text Search resolves it to the actual office. Carrying beds/baths/URL through means cards can show real specs and (later) link out. The price-uncapped partition lets us tell a user "nothing's in budget, the market rate here is ~$X" instead of just "no results". The RentCast counter was a process-global that killed the provider server-wide after 2 searches — resetting per search fixes that. The Apify body was never valid (always 400); the real actor needs a Zillow URL with embedded map bounds. |
| `apartment_finder/instructions.py` | Manager: US-only rejection + optional fields + a `[STRUCTURED_INTAKE]` skip-and-delegate rule. Analyst: top-3→top-5, carries the expanded listing fields + `over_budget`, handles the `no_match_in_budget` shape (no longer collapses it into `NO_RESULTS`), appends a `NO_MATCH_IN_BUDGET` marker. Reviewer: top-3→top-5, preserves the JSON array. Summarizer: made the **ranking authority** — ranks by price+commute+safety and emits a hidden `<!--RANKING:[ids]-->` marker; labels over-budget options; handles the no-match narrative. | The prompts are the contract between the LLM agents and the UI. The Summarizer is the only agent that sees price + commute + safety together, so it should decide the "Top Pick" — not whichever listing happened to load first. The hidden marker lets the UI reorder cards/pins to match the chat without showing machine text to the user. The no-match hardening stops a "listings exist but over budget" case from being mis-reported as "no listings". |
| `apartment_finder/agent.py` | Gave the Summarizer `output_key="final_recommendation"`. | So the bridge can reliably read the Summarizer's full text (including the ranking marker) from session state, rather than trying to reassemble it from streamed tokens. |
| `api/session_manager.py` | F3 structured-intake: `stream_message` accepts `requirements`, persists them via `_compute_requirements` + `append_event(state_delta=…)`, and tells the Manager to delegate. Added `final_recommendation` to the state event. | The frontend's future question-card UI needs a deterministic way to seed requirements without re-parsing free text. Critically, in-memory ADK sessions return a **copy** on read, so mutating state in place is lost — state must be written through an event delta. The `final_recommendation` field carries the ranking to the frontend. |
| `api/server.py` | `ChatRequest` gained optional `requirements`; `/api/health` now reports `degraded` when no listing-provider key is set (no more `local_csv`). | The structured-intake payload needs a transport. Health must reflect the new reality: with no CSV fallback, a missing provider key is a real degradation, not silently OK. |
| `frontend/lib/types.ts` | `Apartment` gained `listing_url`/`listing_source`/`over_budget` (dropped `data_warning`); state SSE event gained `final_recommendation`. | Keeps the new backend data typed end-to-end so the UI can render specs, over-budget badges, and apply the ranking. |
| `frontend/lib/parseApartments.ts` | Carry the new listing fields through; added `parseRanking`/`stripRankingMarker`/`applyRanking`. | The parser is where the dossier becomes card data. The ranking helpers reorder the cards to the Summarizer's order (matched by id, graceful for missing ids) and hide the marker from display. |
| `frontend/hooks/useChat.ts` | Strip the RANKING marker from the displayed bubble via a raw accumulator; on the state event, reorder apartments by the Summarizer's ranking. | Streaming appends tokens, so a naive strip would break once the marker prefix is removed — accumulating raw text and re-stripping each tick keeps the marker invisible. Reordering here makes chat, cards, and map agree on the Top Pick. |
| `frontend/lib/api.ts` | `chatStream` accepts an optional `RequirementsPayload`; defined that type. | The wire format for the F3 structured-intake path. |
| `frontend/components/results/ApartmentCard.tsx` | Rank labels/colors extended to 5; added an amber "Over budget" badge; removed the "Data may be stale" badge. | 5 results now render. Over-budget stretch options need a clear visual flag. The stale badge is dead with the CSV gone. |
| `frontend/components/map/MapView.tsx` | Rank label/color arrays extended to 5. | So all 5 pins get a proper rank label/color instead of falling back to grey. |
| `main.py` | CLI now errors out (instead of "falling back to local CSV") when no provider key is set. | There is no offline fallback anymore, so pretending there is one would mislead. |
| `CLAUDE.md` | Updated env docs (Places API requirement, no CSV, provider required), tool signatures, agent descriptions, removed the Data/CSV section. | The on-disk docs still described the old CSV/geocode behavior; aligned them with the implemented system. |
| `test_tools.py` | Added quota-free offline tests (budget partition, layout filter, US-state validation, per-person budget) + opt-in live tests (`RUN_LIVE=1`) for landmark/commute. | Lets the budget/validation logic be verified without burning RentCast/Apify/Maps quota, while keeping live checks available behind a flag. |

### Decisions & Assumptions
- **Foundation-first ordering**: implemented the schema/contract tickets (F1/F2/F3) before the Phase-1 accuracy fixes, because a prior attempt was discarded for baking Phase-1 assumptions that were incompatible with Phase 2. Locking the expanded signatures up front keeps Phase 2 additive.
- **Summarizer owns ranking, UI follows**: rather than forcing the cards' arbitrary fetch order onto the chat, the Summarizer ranks and the frontend reorders to match — the reasoned pick (commute+price+safety) becomes the visible Top Pick.
- **Layout filter is min-semantics** (`≥ N` beds/baths), per the locked F1 schema, even though one P1-4 acceptance example implies exact-match. Noted in code + plan as a deferred toggle.
- **Apify guardrail moved to run-level query params** (`maxItems`/`maxTotalChargeUsd`) because the actor's own input schema has no item cap.
- **Apify building/community markers** surface as `0bd/0ba` (Zillow gives them a price range, not a single bed count); left as-is with a backlog note since they still carry a valid address + URL.

---

## [2026-06-04 22:15] — UX overhaul: landing page, phase-based layout, ChatGPT-style sidebar, Stop button, landmark pin, route lines, richer map popups, commute RCA fix

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/instructions.py` | Updated ANALYST_PROMPT: added `photos` field to JSON template; changed STEP 2 from a prose commute summary to embedding the raw `check_commutes` JSON verbatim | Commute times were never appearing on cards — the frontend parser expected the raw Google Maps `"rows"` JSON, but the Analyst was writing text like "25 min commute (15 miles)" instead. Requiring the raw JSON fixes the data mismatch. The photos field tells the LLM to carry photo URLs through the dossier. |
| `apartment_finder/tools.py` | `store_requirements`: added landmark geocoding using the existing `googlemaps` client; stores `landmark_lat/lng/name` in session state. `_fetch_apify`: extracts `imgSrc`/`carouselPhotos` photo URL. `_normalize`: passes `photos` array through. | The map needed a pin for the user's commute destination but only had its name as a string. Geocoding it at the moment the user's requirements are saved means coords are available by the time the frontend draws the map. Photos are optional — they only appear when Zillow's Apify scraper returns an image URL. |
| `api/session_manager.py` | Added `_cancel_flags` dict; `cancel_session()` function; cancel check at top of each SSE loop iteration; expanded state event to include `user_requirements`, `landmark_lat`, `landmark_lng`, `landmark_name` | Without cancel support, clicking "Stop" in the UI would close the browser connection but leave the agent pipeline burning Gemini quota in the background. The cancel flag lets the backend generator detect and exit. The extra state fields let the frontend generate a human-readable session title and place the landmark pin — previously that data existed in session state but was never sent to the frontend. |
| `api/server.py` | Added `POST /api/chat/{session_id}/cancel` endpoint; added `GET /api/directions` endpoint that calls Google Maps Directions API, decodes the polyline, and returns `[[lat, lng], ...]` | Two new capabilities the frontend needed: cancelling a running agent run, and fetching road-following route geometry between an apartment and the landmark. Using the same `googlemaps` client already in the project keeps the API-key surface minimal. |
| `requirements.txt` | Added `polyline` package | Google Maps returns encoded polylines (a compact string format). The `polyline` library decodes them back to lat/lng arrays that Leaflet can draw. Without it, the directions endpoint would need to implement the decoding algorithm manually. |
| `frontend/lib/types.ts` | Added `LandmarkInfo` interface; added `photos?: string[]` to `Apartment`; expanded SSEEvent `state` discriminant to include `user_requirements`, `landmark_lat/lng/name` | Adding new data flowing from the backend needs to be typed end-to-end. The `LandmarkInfo` type is reused by both `useChat` and `MapView` so the shape is defined once. |
| `frontend/lib/parseApartments.ts` | Replaced non-greedy regex `\[[\s\S]*?\]` with a bracket-counting parser (`findTopLevelJsonBlocks`) that respects string literals and nesting; applied same fix to commute JSON extraction; added text-format commute fallback | Adding `"photos": []` to the apartment JSON created a nested `]` inside the array. The old non-greedy regex stopped at the first `]` it found — which was inside `photos: []` — so it never captured the complete apartment array. Result: `apartments` was always empty and the results panel never appeared. The bracket-counter handles arbitrary nesting correctly. The text-format fallback handles cases where the LLM doesn't copy the raw JSON faithfully. |
| `frontend/lib/api.ts` | Added optional `signal?: AbortSignal` parameter to `chatStream`; forwards it to the internal `AbortController` | The stop button needed a way to cancel the HTTP stream. `chatStream` already had its own internal abort controller but no way to trigger it from outside. Forwarding an external signal means `useChat` can abort with a single `abortController.abort()` call. |
| `frontend/hooks/useChat.ts` | Added `AbortController` ref; `stopStreaming()` (aborts stream + calls cancel endpoint); `landmark` state; session title generation from `user_requirements`; landmark persistence in localStorage; landmark restoration in `restoreSession` | Centralises all the new functionality that flows from a completed search: knowing when and how to stop, what the commute destination is, and what to label the session in the sidebar. |
| `frontend/components/AppShell.tsx` | Replaced fixed 38/62 two-column layout with a phase-based system: `landing` (full-width centered), `chatting` (full-width chat, no map), `results` (1/3 chat + 2/3 map with CSS transition); sidebar moved from a floating overlay to a persistent flex-row member; removed hamburger button | The old layout always showed both columns even before any search, making the empty right panel feel like broken UI. The phase system means users see a focused landing page first, then a smooth animated transition to results — matching the mental model of "search then see". |
| `frontend/components/chat/ChatPanel.tsx` | Added `phase` prop; landing phase renders large centered title + roomy textarea + suggestion chips; chatting/results phase renders the existing layout; Send button replaced by red Stop square icon when `isStreaming` | The landing UI matches the Claude/ChatGPT pattern users already know: big text field in the middle of the page. The Stop button replaces the send button (not sits alongside it) to avoid confusing users about which to click during a run. |
| `frontend/components/sidebar/ConversationSidebar.tsx` | Rewrote from a sliding drawer overlay to a persistent dark sidebar panel (240px open, 48px collapsed) that is part of the main layout flex row; changed prop from `onClose` to `onToggle`; loads sessions on mount and polls every 3s to catch title updates from the same tab; cleans up `apt_landmark_*` localStorage on delete | Persistent sidebars (like ChatGPT, Claude) are faster to use because they don't require opening a drawer first. The dark background distinguishes it from the main content area. Polling bridges the gap that `storage` events don't fire for writes in the same browser tab — needed so session titles update live after a search completes. |
| `frontend/components/map/MapView.tsx` | Added amber star landmark pin; rank-colored apartment pins (`makeAptIcon` by index); route polyline fetched on popup open (dashed placeholder while loading, road-following polyline on success); enhanced popup with photo, rank badge, commute info | The map previously showed only price and address on click. The redesign makes each pin self-contained: users can see the ranking, commute time, and a photo without switching to the cards tab. The route line gives a spatial sense of the commute that a duration number alone doesn't convey. Routes are fetched lazily (only when a pin is opened) to avoid burning Directions API quota for all apartments upfront. |
| `frontend/components/results/ResultsPanel.tsx` | Added `landmark?: LandmarkInfo` prop; passes it through to `MapView`; changed default tab from `"cards"` to `"map"` | The map is now the richer view (landmark pin, route lines, photo popups), so it makes sense to show it first. Landmark needs to be threaded from `AppShell` → `ResultsPanel` → `MapView`. |

### Decisions & Assumptions
- **Bracket-counting parser vs. stricter JSON schema in instructions**: Tightening the LLM prompt to output only what the parser expects is brittle — models drift. The bracket-counter is defensive infrastructure that tolerates any valid JSON the LLM produces, however nested.
- **Route polylines are fetched lazily on popup open**: Fetching routes for all apartments upfront would cost one Directions API call per listing every time results load. Lazy fetching means the user only pays for routes they actually look at.
- **Straight dashed line shown as placeholder while route loads**: Gives immediate visual feedback that the routing is in progress, rather than showing nothing until the fetch completes.
- **Session titles polled every 3s (same-tab limitation)**: The `storage` event only fires in *other* tabs. Since `useChat` writes the session title in the same tab, the sidebar would never see the update via events alone. 3s polling is a lightweight workaround — not ideal but invisible to the user.
- **Stop cancels both frontend stream and backend pipeline**: Cancelling only the frontend SSE connection would leave the ADK agent runner consuming Gemini quota in the background with no way to retrieve results. The backend cancel flag exits the generator cleanly.
- **`polyline` package chosen over manual decode**: Google's polyline encoding algorithm is well-specified but ~20 lines of bit-manipulation code. The `polyline` PyPI package is battle-tested and adds negligible weight to the environment.

---

## [2026-06-04 18:32] — Reduce Gemini rate limits via model split; fix 5 UI/pipeline bugs

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/agent.py` | Replaced single shared `model = Gemini(...)` with `gemini_model` (Reviewer only) and `openai_model` (Manager, Analyst, Summarizer) using ADK's `LiteLlm` wrapper | The Gemini 2.5 Flash free tier allows very few requests per minute. Running all 4 agents through it means every search burns 4+ quota slots. Moving 3 agents to OpenAI (gpt-4o-mini) cuts Gemini usage by ~75%. The Reviewer must stay on Gemini because ADK's built-in `google_search` grounding tool is Gemini-native and silently fails on other providers. |
| `requirements.txt` | Added `litellm` | ADK's `LiteLlm` wrapper delegates to the `litellm` package under the hood. Without it installed, importing `LiteLlm` throws `ModuleNotFoundError` at startup. |
| `apartment_finder/tools.py` | Changed `fetch_apartments` provider loop: empty `[]` results now `continue` to next provider instead of returning "no results" immediately | RentCast was returning an empty list for expensive markets like Seattle (no listings under budget) rather than raising an error. The old code treated empty-but-successful as final, never trying Apify or the local CSV. Now empty results are treated like a soft miss — the chain keeps trying until all providers are exhausted. |
| `api/session_manager.py` | Added tool-call-based status event detection in `stream_message` — inspects `part.function_call.name` to infer which sub-agent is active | ADK's `event.author` field doesn't reliably carry sub-agent names for agents nested inside a `SequentialAgent`. The pipeline status bar (Analyst, Reviewer steps) was never lighting up. Detecting `fetch_apartments`/`check_commutes` calls → Analyst active, and `google_search` calls → Reviewer active gives reliable status signals regardless of how ADK sets `author`. |
| `frontend/components/results/ResultsPanel.tsx` | Changed `next/dynamic` import for `MapView` to wrap the named export as `{ default: m.MapView }` | `next/dynamic` expects a module with a `default` export. Named exports (`export function MapView`) don't satisfy that contract — React's lazy loader called `render` on the module object itself, which is not a function. Wrapping it as `{ default: m.MapView }` makes it look like a default export to `dynamic`. |
| `frontend/hooks/useChat.ts` | Added `currentBubbleId`/`currentBubbleAuthor` tracking so each agent author gets its own message bubble; replaced hardcoded `assistantMsgId` references | Manager and Summarizer both emit non-intermediate tokens, but they were all appended to the same placeholder bubble — the user saw one concatenated block of text with no visible handoff. Now when the token author changes, a new empty bubble is created and subsequent tokens go into it, giving each agent its own distinct chat message. |
| `frontend/hooks/useChat.ts` | Added `useEffect` to persist `apartments` to `localStorage` under `apt_apartments_<sessionId>`; `restoreSession` now reads it back instead of always calling `setApartments([])` | When revisiting a past session from the sidebar, the chat messages were restored but the result cards and map were blank. Apartments are now stored alongside messages and reloaded on session restore. Only sessions created after this change will have persisted apartments. |
| `frontend/package.json` + `package-lock.json` | Downgraded `react-leaflet` from `^5.0.0` to `^4.2.1`; `@react-leaflet/core` also downgraded from `3.0.0` to `2.1.0` | `react-leaflet@5` dropped React 18 support and requires React 19. The project uses React 18.3.1. When the Map tab was clicked, React's `updateContextConsumer` called `render` on an object that isn't a function — because v5 uses React 19's new context API internally. v4.2.1 is the latest React 18-compatible release and has the same component API. |

### Decisions & Assumptions
- **`gemini-2.5-flash-lite` chosen over `gemini-2.5-flash` for the Reviewer**: Only 1 of 4 agents remains on Gemini, so the lighter model reduces free-tier pressure further. The Reviewer only does web search + summarization — it doesn't need the full Flash model's reasoning depth.
- **`gemini-2.0-flash` was ruled out**: Deprecated and shut down June 1, 2026. Cannot be used.
- **Tool-call detection uses `seen_authors` guard**: Both the author-based and tool-call-based status triggers write to `seen_authors`, so if ADK *does* surface `author="analyst"` on some events, duplicate status events are suppressed.
- **Apartment persistence is forward-only**: Sessions completed before this commit have no `apt_apartments_*` entry in localStorage. Restoring them will show messages but not result cards — this is expected and not a regression.

---

## [2026-06-01 16:16] — Build React UI: Next.js frontend + FastAPI backend bridge, fix 429 crash and agent status visibility

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `api/__init__.py` | New file (empty package marker) | Makes `api/` a proper Python package so `api.server` and `api.session_manager` can be imported. |
| `api/session_manager.py` | New file — wraps ADK `InMemoryRunner`, manages one session per user conversation, streams SSE events (status, token, state, done, error) | The CLI's `main.py` kept one runner alive for one user. For a multi-user web app each browser tab needs its own isolated runner+session. This module owns that registry in a dict. Each agent author name (`analyst`, `reviewer`, etc.) is mapped to a human label emitted as a `status` event so the frontend can show which pipeline step is running. `runner.run_async()` is wrapped in `try/except` so a 429 rate-limit error yields a friendly `error` SSE event instead of crashing the ASGI process. |
| `api/server.py` | New file — FastAPI app with CORS, three REST endpoints + SSE streaming | The ADK runner is Python-only; the React frontend needs an HTTP interface. FastAPI's `StreamingResponse` with `text/event-stream` media type lets the browser receive real-time agent events as they happen, rather than waiting for the full response. Three endpoints: `POST /api/sessions` (create session), `POST /api/chat/{id}` (stream events), `GET /api/sessions/{id}/state` (fetch session state for card hydration). |
| `requirements.txt` | Added `fastapi` and `uvicorn[standard]` | Runtime dependencies for the new API server. |
| `package.json` | Added `dev`, `api`, `ui` scripts; added `concurrently` devDependency | Lets developers start both servers with a single `npm run dev` command from the repo root instead of managing two terminal windows manually. Uses `127.0.0.1` explicitly in the uvicorn command to avoid the Windows IPv6 (`::1`) resolution mismatch. |
| `frontend/` | New directory — entire Next.js 14 app (App Router, TypeScript, Tailwind CSS v3) | The CLI-only interface needed a visual product that any user could open in a browser. Next.js 14 (not 16) was used because Node v18 is installed on this machine — Next.js 16 requires Node ≥ 20 and its `@tailwindcss/oxide` binary would fail to install. |
| `frontend/next.config.mjs` | New file — proxies `/api/*` to `http://127.0.0.1:8000` | Next.js dev server and FastAPI run on different ports. A proxy rewrite lets the frontend call `/api/...` without CORS headers. `localhost` was replaced with `127.0.0.1` because Windows Node resolves `localhost` to IPv6 `::1`, while uvicorn binds to IPv4, causing `ECONNREFUSED`. |
| `frontend/tailwind.config.ts` | New file — Tailwind v3 config with custom design tokens | Defines the app's color palette (primary blue `#1A56DB`, accent sky `#0EA5E9`, surface whites, muted gray) and custom keyframe animations (`fadeIn`, `slideUp`, `pulseDot`) used by the agent status dots. |
| `frontend/app/globals.css` | New file — Tailwind directives, Google Fonts Inter, leaflet CSS import, scrollbar styling | Tailwind v4 uses `@import "tailwindcss"` but v3 needs `@tailwind base/components/utilities`. Leaflet's CSS must be imported globally (not scoped) or the map tiles render without positioning. |
| `frontend/app/layout.tsx` | Replaced scaffold — sets page title/description, clean `h-full` body | The scaffold imported Geist fonts (unavailable in Next.js 14) and added dark-mode variables incompatible with our design system. Simplified to just set metadata and pass through children. |
| `frontend/app/page.tsx` | Replaced scaffold — renders `<AppShell />` | Single-entry point; all logic lives in components. |
| `frontend/components/AppShell.tsx` | New file — 38/62 split layout, wires `useChat` hook to panels | Two-column CSS grid: left 38% for chat, right 62% for results. Owns the sidebar open/close toggle. Passes chat state (messages, apartments, agentStatus) down to both panels so they stay in sync. |
| `frontend/components/chat/ChatPanel.tsx` | New file — message list, suggestion chips, streaming textarea input | The `AgentStatus` component is placed **outside** the scrollable messages div, between messages and the input bar. This means the pipeline tracker is always visible while the agent works, regardless of how far the user has scrolled. |
| `frontend/components/chat/MessageBubble.tsx` | New file — renders user (right-aligned blue) and assistant (left-aligned, with avatar icon) messages | Distinguishes user and AI messages visually so the conversation is scannable at a glance. Empty assistant messages are hidden to avoid a blank bubble flash at stream start. |
| `frontend/components/chat/AgentStatus.tsx` | New file (then redesigned) — 4-step pipeline tracker (Requirements → Listings → Safety → Summary) | The original design was a small pill buried in the scroll area — easy to miss. The redesign shows a permanent step-by-step pipeline bar with the active step highlighted in blue, completed steps shown with a green checkmark, and animated dots next to the current step label. Because it lives outside the scroll container, it's always on screen while the agent is working. |
| `frontend/components/results/ResultsPanel.tsx` | New file — tabs (Cards / Map), empty state with 3-step explainer | Conditionally shows apartment cards or the Leaflet map based on the selected tab. Uses `next/dynamic` with `ssr: false` to load the map only in the browser — Leaflet uses `window` which doesn't exist during server-side rendering. |
| `frontend/components/results/ApartmentCard.tsx` | New file — price, address, commute badge (color-coded), safety note, stale-data warning | Ranks apartments with labels ("Top Pick", "Runner-up", "Budget Pick") and colour codes the commute badge green/amber/red based on drive time. Cards update the map highlight on hover via the shared `hoveredId` state. |
| `frontend/components/results/CommuteBadge.tsx` | New file — shows drive time + distance with colour coding | Green ≤ 20 min, amber 21–40 min, red > 40 min. Makes commute quality immediately readable without needing to read numbers. |
| `frontend/components/map/MapView.tsx` | New file — react-leaflet map with custom div icons, FitBounds effect | SSR-disabled via dynamic import. Fixes Leaflet's broken default icon path (webpack strips the `_getIconUrl` method) by manually setting CDN URLs. Custom `divIcon` markers instead of PNG images so highlighted state can be applied with CSS without loading extra assets. `FitBounds` component auto-zooms to fit all apartment pins on mount. |
| `frontend/components/sidebar/ConversationSidebar.tsx` | New file — slide-in drawer with session history from localStorage | Each search is stored in `localStorage` by `useChat` when the session is created. The sidebar reads this on open and shows the first 60 characters of the first message as the session title, plus a relative timestamp. |
| `frontend/hooks/useChat.ts` | New file — SSE stream parser, session lifecycle, apartment state | Uses `fetch` + `ReadableStream` (not `EventSource`) for full control over the connection. Parses `data: {...}` SSE lines: `status` events update the pipeline tracker, `token` events append to the assistant message (only from `manager` or `summarizer` authors — intermediate agents' tokens are filtered out), `state` events trigger apartment card parsing, `error` events replace the assistant placeholder with a human-readable error message, `done` clears the status bar. |
| `frontend/lib/types.ts` | New file — shared TypeScript interfaces | Single source of truth for `Apartment`, `CommuteInfo`, `ChatMessage`, `AgentStatusEvent`, and the `SSEEvent` discriminated union. |
| `frontend/lib/api.ts` | New file — typed fetch wrappers for FastAPI endpoints | Abstracts session creation and SSE stream setup so hooks don't contain raw fetch logic. |
| `frontend/lib/parseApartments.ts` | New file — extracts apartment data from ADK session state | The Analyst agent writes a prose + JSON dossier to session state. This parser uses regex to find embedded JSON arrays in the text, validates that they have `latitude`/`longitude` fields, and maps them to typed `Apartment` objects. Also parses Google Maps Distance Matrix responses to attach commute info to each card. |

### Decisions & Assumptions
- **Next.js 14 instead of 16**: Node v18 is installed on this machine; Next.js 16 requires Node ≥ 20. Tailwind CSS v4's `@tailwindcss/oxide` binary also requires Node 20 and will fail to compile. All Tailwind v4-specific syntax (`@import "tailwindcss"`, `@theme`) was replaced with v3 equivalents.
- **`127.0.0.1` instead of `localhost` in proxy and uvicorn**: Windows resolves `localhost` to `::1` (IPv6) in Node.js but uvicorn binds to `0.0.0.0` / `127.0.0.1` (IPv4) by default, causing `ECONNREFUSED` on the first API call. Hardcoding the IPv4 address in both the Next.js proxy config and the uvicorn startup command eliminates the ambiguity.
- **Agent status moved outside scroll area**: The pipeline tracker was originally inside the scrollable messages div — it appeared at the bottom of the message list and was invisible if the user had scrolled or if the chat was short. Moving it to a fixed slot between the message list and the input bar means it's always on screen while the agent is working.
- **Only `manager` and `summarizer` tokens are rendered**: The Analyst, Reviewer, and Research Team agents produce internal reasoning text (tool call results, JSON dossiers) that isn't meant for the user. Filtering by author name ensures only the final Manager Q&A responses and Summarizer recommendation text appear in the chat.
- **429 error is caught in `session_manager.py`, not `server.py`**: The exception originates inside the `runner.run_async()` async generator. If caught at the FastAPI layer the generator is already broken; catching it at the source lets us yield a proper `error` SSE event and close the stream cleanly before FastAPI sees it.
- **Session history is localStorage-only**: Full message history restoration from the backend would require storing every message — out of scope for this session. The sidebar shows past sessions for reference and allows starting fresh; replaying a past session resets to a new blank conversation.

---

## [2026-06-01 13:34] — Add API cost guardrails for RentCast and Apify providers

### Changes

| File | What Changed | Why (Plain English) |
|------|-------------|---------------------|
| `apartment_finder/tools.py` | Added `_RENTCAST_MONTHLY_LIMIT`, `_RENTCAST_MAX_PER_RUN`, `_RENTCAST_USAGE_FILE` constants; added `_rentcast_run_count` module-level counter; added `_load_rentcast_usage()` and `_save_rentcast_usage()` helpers; updated `_fetch_rentcast()` to enforce both limits before making any HTTP call | RentCast's free tier only allows 50 calls per month. Without a guardrail, a handful of test runs could exhaust the whole month's quota silently. The per-run cap (2 calls) prevents a single runaway session from burning most of the budget, while the monthly cap (50) automatically reroutes to Apify the moment that ceiling is hit — no manual monitoring needed. Usage is persisted to `data/rentcast_usage.json` so the count survives process restarts and resets itself automatically when the calendar month rolls over. |
| `apartment_finder/tools.py` | Added `_APIFY_MAX_BUDGET_USD`, `_APIFY_COST_PER_1000`, `_APIFY_MAX_ITEMS` constants; replaced hardcoded `maxItems: 5` with `safe_max_items = min(5, _APIFY_MAX_ITEMS)` in `_fetch_apify()` | The Zillow scraper actor costs $2.30 per 1,000 results. At 5 results per call this is cheap, but if `maxItems` is ever raised during development the cost could spike. The guardrail derives the maximum safe item count from the $0.20/run budget (`0.20 / 2.3 * 1000 = 86`) and enforces it at the call site, so future changes to the requested count can never accidentally exceed the budget. |
| `apartment_finder/tools.py` | Added `from datetime import datetime` import | Required by `_load_rentcast_usage()` to stamp and compare the current calendar month. |

### Decisions & Assumptions
- **Usage counter increments after a successful HTTP response**, not before. This means a failed or timed-out request doesn't consume a quota slot — only actual successful API calls are counted against the monthly limit.
- **Monthly reset is implicit**: `_load_rentcast_usage()` checks the stored month string against the current month. If they differ (new month), it returns a fresh zero-count object. No cron job or manual reset is needed.
- **Apify `maxItems` is currently 5**, which is already far below the 86-item guardrail ceiling. The guardrail is latent protection — it activates only if the requested count is ever raised.
- **Per-run RentCast counter (`_rentcast_run_count`) lives in module memory** and resets when the process restarts. This maps naturally to "one agent session = one run" since `main.py` starts a fresh process each time.

---
