# Session Summary Log

_Auto-generated by Claude Code at session end. Each block = one session._

---

## Session: 2026-07-15 → 2026-07-16
**Start:** approx. 2026-07-15 evening | **End:** 2026-07-16 01:53 | **Duration:** ~5-6 hrs

### What We Built / Changed
Two-part session: first shipped Phase 3.5 (Langfuse tracing) + FU-1 (follow-up requirement
carry-forward), then — on request — planned and executed Phase 3.75, a from-scratch Cloud Run +
Vercel deployment of the app for the first time. Phase 3.5 wired `GoogleADKInstrumentor` into
`apartment_finder/tracing.py`, deliberately skipping the plan's originally-scoped second LiteLLM
callback after live-verifying the ADK instrumentor alone already captures LiteLLM-backed agent
calls in the same trace tree — proven by a real trace showing `model: "openai/gpt-4o-mini"`. FU-1
fixed `store_requirements` to carry forward unstated fields from the previous turn instead of
silently resetting them, after confirming (by reading ADK's `Runner._find_agent_to_run` source)
that the originally-suspected ADK state-continuity bug didn't actually exist. Also installed the
official `langfuse/skills` Claude Code skill into `.claude/skills/langfuse/` per the user's
request. That work was committed (`cf49199`). The deployment work that followed is the bulk of the
session: went through `EnterPlanMode` to research real blockers (session persistence on an
ephemeral/scale-to-zero backend, quota-counter durability, Vercel's SSE timeout), got real pricing
numbers instead of assumptions (Cloud Run's always-free tier is *permanent*, not the 90-day trial —
`min-instances=0` is genuinely $0/mo), then built: a pluggable Firestore/file usage-counter backend,
a full frontend session-recovery shim (a lost session was previously *permanently* bricking the
conversation — found only by tracing the actual code path), a `Dockerfile`, and configurable CORS.
Container testing (once Docker Desktop was up) caught two real bugs that unit tests never could:
`requirements.txt` was unpinned and Cloud Build silently resolved `google-adk` 2.4.0 against a
codebase tested only on 1.19.0 (fixed by pinning), and `tracing.init()` had no broad exception
handler, so a bad Langfuse URL could crash the entire server at import time before it ever started
listening (fixed, and it saved the actual Cloud Run deploy from doing exactly that). Actually
deployed to Cloud Run (`gen-lang-client-0523627966`, `us-central1`) — Firestore created, 8 secrets
pushed to Secret Manager and verified byte-for-byte clean, IAM granted, service live and verified
end-to-end including a real Langfuse trace from production. Session ends mid-Vercel-import: hit two
snags (the repo's GitHub default branch, `master`, has no `frontend/` directory at all — 10 commits
behind `v2-prod` — so Vercel's initial deploy failed; and the Production Branch setting turned out
to live under Settings → Environments → Production → Branch Tracking, not the Git tab). Both were
diagnosed but not yet resolved on the Vercel side when the user asked to checkpoint via
session-summary + commit-push.

### Changes Made
No `changelogExplanation.md` entry for this session yet (skipped per explicit user instruction to
run `/session-summary` then `/commit-push` in that order, not `/code-changelog`). High-level, from
`git diff HEAD --stat` (8 files modified, 2 new, 721 insertions / 180 deletions, all uncommitted at
time of writing):
- **`apartment_finder/tracing.py`**: wrapped everything after the Langfuse key check in a broad
  `except Exception` (previously only caught `ImportError` — the startup-crash bug above); added
  `flush()`, called from a new FastAPI `lifespan` shutdown handler in `api/server.py` so
  Cloud Run's SIGTERM-on-scale-to-zero doesn't drop the last batched traces.
- **`apartment_finder/tools.py`**: new `_load_usage`/`_save_usage` pair behind a `USAGE_STORE=file|
  firestore` env switch; `_load_rentcast_usage`/`_save_rentcast_usage`/`_load_places_usage`/
  `_save_places_usage` became thin delegates with their signatures unchanged (required — 
  `test_tools.py` monkeypatches those exact module attributes).
- **`api/server.py`**: `ALLOWED_ORIGINS` env var appended to CORS origins; `/api/health` now
  reports `usage_store` and `tracing_enabled`; new lifespan handler (above).
- **`frontend/lib/api.ts`**: `chatStream` now checks `res.ok` and synthesizes a proper SSE error
  instead of piping a raw 4xx/5xx body through; new `isSessionAlive()` liveness probe.
- **`frontend/hooks/useChat.ts`**: `sendMessage` restructured around a `runTurn`/`ensureSession`
  split so a `"Session not found."` SSE error (previously painted into the chat bubble forever,
  with `sessionIdRef` never cleared) now triggers one transparent re-create-and-replay; new
  `rekeySession()` moves the sidebar entry + `apt_*` localStorage keys onto the fresh session id so
  no duplicate conversation appears; `restoreSession` now probes liveness before adopting a cached
  id instead of trusting it blindly.
- **`Dockerfile`** (new), **`.dockerignore`** (new): `python:3.10-slim`, API-only image,
  `exec uvicorn ...` so it runs as PID 1 for correct SIGTERM handling — verified live (1.4-1.7s
  graceful shutdown).
- **`requirements.txt`**: went from fully unpinned to pinned on the exact tested versions
  (`google-adk==1.19.0`, `litellm==1.80.5`, `openai==2.8.1`, etc.) plus `google-cloud-firestore`;
  dropped `pandas` (only the retired `preprocessing.py` used it).
- **`test_tools.py`**: 3 new offline tests for the usage-store delegate (fake-Firestore round-trip,
  month rollover, fail-open on a simulated outage) — 26/26 offline tests passing throughout.
- **`CLAUDE.md`**: new Deployment section (env vars, gcloud deploy command, the `--env-file`
  quote-stripping gotcha, known gaps) plus a correction of an earlier session's own inflated
  "30/30 tests" claim in the backlog file (actual count was 23 at the time).

### Problems Encountered & How They Were Resolved
- **Assumed ADK had a session-continuity bug for FU-1 → it didn't.** Read `Runner._find_agent_to_run`
  in the installed `google-adk` source directly rather than trust the assumption; the real bug was
  entirely in `store_requirements` not merging with prior state.
- **Two Langfuse doc URLs I guessed from memory 404'd** (`/docs/scores/model-based-evals`,
  `/docs/evaluation/features/evaluation-methods/custom-scores`) — refetched from the real sitemap.
  Reinforced why the langfuse skill's "documentation first" rule exists.
- **Vercel's 300s function timeout — I initially misremembered it as 60s.** Checked the actual docs
  before designing around it; still relevant (429-retry path can exceed even 300s) but the number
  mattered for deciding SSE routes straight to Cloud Run instead of through a Vercel function.
- **Docker's `--env-file` doesn't strip quotes** (unlike python-dotenv) → a locally-untested
  `LANGFUSE_BASE_URL="..."` with literal quotes crashed the container, which is what originally
  surfaced the missing-broad-except bug in `tracing.py`. Documented as a standing gotcha.
- **`requirements.txt` was unpinned** → Cloud Build resolved `google-adk` 2.4.0 vs the tested 1.19.0,
  surfacing first as a cryptic `No module named 'orjson'` from litellm's proxy submodule. Diagnosed
  by directly diffing installed versions between the local venv and the container, then pinned.
- **First Vercel import failed** because GitHub's default branch (`master`) has no `frontend/`
  directory (10 commits behind `v2-prod`, from before the Next.js UI existed). Not yet resolved —
  see below.
- **Vercel's Production Branch setting wasn't where expected** (Settings → Git only shows Deploy
  Hooks in the current UI) — found the real location, Settings → Environments → Production →
  Branch Tracking, by checking Vercel's docs rather than guessing further.

### What's Left / Next Steps
- **Finish the Vercel import**: set default branch to `v2-prod` on GitHub (or fix Production Branch
  under Environments in Vercel), set `BACKEND_URL` + `NEXT_PUBLIC_BACKEND_URL` to the live Cloud Run
  URL (`https://apartment-finder-api-696222819704.us-central1.run.app`) **before** the build runs
  (`NEXT_PUBLIC_*` is inlined at build time).
- **Set `ALLOWED_ORIGINS`** on the Cloud Run service to the resulting Vercel origin once known — CORS
  will reject the frontend until this is done.
- **Run the P3.75-5 post-deploy smoke test**: a full real search from the public URL, confirm
  Firestore counters actually increment (only verifiable once real traffic hits it), confirm one
  Langfuse trace tree for the whole search. This also doubles as the long-deferred live spot-check
  for FU-1/FU-5/FU-6 (prompt-level fixes that offline tests structurally can't cover).
- **Known, deliberately-deferred gaps** (documented in the backlog, not blockers): usage-counter
  read-modify-write can still lose an update under concurrency (Firestore only fixed torn reads);
  `_sessions` never evicts (mitigated by scale-to-zero); `datetime.now()` is naive-local so the
  monthly counter's rollover boundary differs UTC-on-Cloud-Run vs local-on-dev.
- This session's uncommitted work is about to be committed via `/commit-push` immediately after this
  summary, per explicit user instruction.

### Session Stats
- Commits made this session: 1 (`cf49199`, Phase 3.5 + FU-1) — Phase 3.75 deployment work still
  uncommitted at time of writing, about to be committed next.
- Files changed (uncommitted, Phase 3.75): 8 modified, 2 new (`Dockerfile`, `.dockerignore`)
- External resources created: 1 Cloud Run service, 1 Firestore database, 8 Secret Manager secrets,
  2 IAM bindings — all live in `gen-lang-client-0523627966`.

---

## Session: 2026-07-13 → 2026-07-14
**Start:** approx. 2026-07-13 evening | **End:** 2026-07-14 00:46 | **Duration:** ~4–5 hrs

### What We Built / Changed
Implemented Phase 3 of the backlog (P3-1/P3-2: "View listing" and "Directions in Google Maps" action buttons on cards + map popups), then spent most of the session doing live-bug RCA and fixes triggered by the user's own testing. A real search ("2B2B Redmond WA near Microsoft, $1800/person") surfaced a serious bug: the map/cards correctly showed 5 real listings, but the chat told the user "no apartments found" — traced to the Analyst→Reviewer→Summarizer pipeline carrying its "did we find anything" decision purely as free-text markers with a fragile substring check. Fixed with a deterministic pattern the user explicitly asked for: `fetch_apartments` now writes the true outcome (`search_status`) into session state in code, and new `before_agent_callback`s on the Reviewer/Summarizer gate on that value instead of trusting LLM-relayed prose (confirmed against the installed `google-adk` API). Same session, a live re-test surfaced two more bugs in the roommate/per-person math — the Summarizer was dividing an already-correct per-person number a second time, and after a first fix, copying a hardcoded "2 ways" from the fix's own example text regardless of the real headcount. Both were resolved by moving the number *and* the exact wording fully into code. A third bug (the Manager fabricating a roommate count the user never stated) turned out to be a genuinely different class — no code-level value exists to check it against, so the fix is a mandatory clarifying question instead of a code gate. Also discussed and scheduled Langfuse observability (to catch this whole bug family faster next time) as a new backlog item before Phase 4.

### Changes Made
Full detail in [`changelogExplanation.md`](changelogExplanation.md) under the `[2026-07-14 00:45]` entry. High-level:
- **Frontend** (`ApartmentCard.tsx`, `MapView.tsx`, `ResultsPanel.tsx`): View-listing + Directions buttons on cards and map popups (P3-1/P3-2). Verified with a temporary mock route driven by Playwright (screenshotted, then deleted) rather than a live agent run, to avoid burning API quota.
- **Backend** (`tools.py` +182 lines, `agent.py`, `instructions.py`): `search_status` deterministic state flag + provider-merge fix (RentCast→Apify fallback now triggers on thin in-budget results, not just an empty pool) (FU-3/FU-4); `_attach_per_person_price` computing both the number and the full "$X per person, split N ways" label in code (FU-5); new Manager prompt section requiring a clarifying question instead of guessing an occupant headcount (FU-6); two new `before_agent_callback`s on Reviewer/Summarizer enforcing the `search_status` gate.
- **Tests** (`test_tools.py` +178 lines): 9 new offline tests covering provider merge, `search_status` recording, per-person price/label correctness, the fallback-recommendation builder, and both new callbacks. 24 total offline tests pass.
- **Plan file** (`~/.claude/plans/i-want-to-extend-enhance-tidy-pinwheel.md`): Phase 3 marked done; FU-3 through FU-6 logged with full root-cause detail; new "Phase 3.5 — Observability (Langfuse)" section added, explicitly scheduled before Phase 4.

### Problems Encountered & How They Were Resolved
- **"No apartments found" shown despite real listings on the map (FU-3):** pipeline's status routing was pure LLM-relayed prose across 3 hops with a loose substring check → collapsed a real result into a false negative. Fixed with a code-computed `search_status` flag + `before_agent_callback` gates on Reviewer/Summarizer (skip-on-true-negative, correct-on-false-negative).
- **RentCast's thin free-tier pool silently blocked Apify from ever being tried (FU-4):** provider chain stopped at the first non-empty pool, even a sparse one. Fixed to merge-and-continue until enough in-budget results are found, bounded by existing guardrails (no new cost).
- **Per-person rent divided twice (FU-5):** Summarizer's own arithmetic in prose re-divided an already-correct number. Fixed by computing it once in code and telling the LLM to copy it verbatim.
- **"Split 2 ways" label wrong even when the number was right (FU-5 follow-up, found on the very next live run):** the fix above still left "split N ways" wording for the LLM to write, and it copied a hardcoded example literal instead of the real count. Fixed by moving the whole phrase into code too — lesson: even a "copy this verbatim" instruction isn't safe if the example contains a number that varies per case.
- **Manager fabricated a roommate count that was never stated (FU-6):** user typed "...under 1800 per person" with no headcount; Manager silently guessed `roommates=2`. This one has no code-level backing value (it's a fact only the user knows), so the fix is a mandatory clarifying-question requirement in the Manager's prompt instead of a code gate — a useful distinction to remember for future prompt bugs in this repo.
- **Local Playwright browser version mismatch** while verifying the Phase 3 buttons — installed `playwright` npm package expected a `chromium_headless_shell` revision not present in the local cache. Worked around by pinning `executablePath` to the cached `chromium-1217` full-browser build.
- **Next.js App Router silently 404'd a temp test route** named `app/__p3_verify/` — single-underscore-prefixed folders are treated as private/excluded from routing. Renamed to `app/p3verify/` and it worked.

### What's Left / Next Steps
- **Nothing committed yet this session** — this summary/changelog pass runs immediately before the user's requested `/commit-push`.
- **Live spot-checks recommended** (none of these are offline-testable — they're LLM judgment calls, not pure functions):
  - Confirm the Manager now asks a clarifying question for "budget of $X per person" with no headcount, instead of guessing (FU-6).
  - Confirm a real `no_match_in_budget` search no longer produces the false "no results" bail-out text (FU-3) — wasn't reproduced live this session to avoid burning RentCast/Gemini/OpenAI quota.
  - Confirm the Summarizer's per-person prose now matches `per_person_label` exactly on a live roommate search (FU-5).
- **`roommates`→`occupants` rename** — user flagged the field name is ambiguous (does it include the user?). Addressed for now with a clearer definition inline in the Manager prompt; a full rename across ~10+ files (backend schema, prompts, frontend types/components, localStorage keys) is still open, pending the user's decision — it's a bigger refactor with real regression risk on an already-verified feature (P2-3).
- **Langfuse observability (new Phase 3.5, blocks Phase 4)** — user needs to create a Langfuse account/API keys before this can be wired up; not something to provision unattended.
- **`travelmode` hardcoded to "driving"** on the new Directions button — deferred until commute mode is threaded end-to-end to the frontend (currently not on `CommuteInfo`).
- **Phase 4 (Mapbox migration + production UI polish)** is next after Langfuse, per the plan file.

### Session Stats
- Commits made: 0 (about to run `/commit-push` next)
- Files changed: 8 (`agent.py`, `instructions.py`, `tools.py`, `changelogExplanation.md`, `MapView.tsx`, `ApartmentCard.tsx`, `ResultsPanel.tsx`, `test_tools.py`) — 550 insertions, 22 deletions in the working tree (per `git diff HEAD --stat`, before this file and `changelogExplanation.md`'s own diff)
- Offline tests: 24/24 passing (`python test_tools.py`)
- Plan file + 1 new memory file (`project_status_routing_fix.md`) updated to record FU-3 through FU-6 and the Langfuse decision

---

## Session: 2026-06-24
**Start:** approx. 2026-06-23 evening | **End:** 2026-06-24 00:08 | **Duration:** ~3–4 hrs

### What We Built / Changed
Implemented **Phase 2** of the ApartmentFinder backlog (P2-1 through P2-5) plus two critical bug fixes that surfaced during testing. P2-1 was already in (verify-only); P2-2 (multi-area search) was parked by the user due to input-surface ambiguity. The substantive new features were: roommate/per-person economics on cards (P2-3), a full proximity amenities tool with named/category/transit branches and a quota guardrail (P2-4 including a Tier 1 transit fix — Google Places typed `searchNearby` to find the nearest actual Caltrain/bus station per listing instead of geocoding a city centroid), and the RequirementCards optional-preferences UI on the landing page (P2-5, with a two-path bridge so nested proximity lists can reach `store_requirements` deterministically). Along the way we hit two critical production bugs: a P0 blank-screen issue (Gemini 503 → pipeline abort → `session_manager` discarded Analyst results by early-returning on error — fixed with a result-salvage pattern) and the SSE "frozen on Manager" bug (Next.js dev buffers `text/event-stream` on both the rewrites proxy and App Router route handlers — fixed by routing the browser SSE fetch directly to uvicorn via `NEXT_PUBLIC_BACKEND_URL`). Also mitigated Gemini 503 long-term: batched the Reviewer into one `google_search` call, narrowed `retry_config` to 429-only, and added `ResilientGemini` wrapper with fallback to `gemini-2.5-flash`.

### Changes Made
Full detail in [`changelogExplanation.md`](changelogExplanation.md) under the `[2026-06-24 00:06]` entry. High-level:
- **Backend** (`tools.py` +289 lines, `agent.py`, `instructions.py`, `api/session_manager.py`, `api/server.py`): `find_nearby_amenities` tool (named/category/transit kinds + guardrail + cache); `ResilientGemini` wrapper; batched Reviewer prompt; result-salvage error handling; `pending_optional` seeding for optional-only card payloads; CORS regex for SSE bypass.
- **Frontend** (`RequirementCards.tsx` new, `ChatPanel.tsx`, `useChat.ts`, `ApartmentCard.tsx`, `ResultsPanel.tsx`, `AppShell.tsx`, `api.ts`, `parseApartments.ts`, `types.ts`, `next.config.mjs`, `.env.local` new): collapsible preferences card panel; per-person rent display; proximity emerald badges; `STREAM_BASE` SSE direct-fetch bypass.
- **Tests** (`test_tools.py` +266 lines): 7 new offline tests covering roommates default, proximity branches, guardrail, transit type mapping, pending_optional fold-in, ResilientGemini fallback. 12 total offline tests pass.

### Problems Encountered & How They Were Resolved
- **Gemini 503 → blank UI (P0):** Reviewer hit "model overloaded" → ADK retried ~166 s → pipeline aborted → `session_manager` early-returned, skipping the `state` event → Analyst listings discarded. Fixed with `run_error = error_msg; break` + always-emit-state salvage pattern.
- **SSE frozen on "Manager" for entire run duration:** Next.js dev buffers `text/event-stream` on both the `rewrites()` proxy and App Router route handlers. Confirmed via Playwright: "Manager" at 12 s and 31 s, full dump at ~37 s. Fixed by setting `NEXT_PUBLIC_BACKEND_URL` so the browser fetches SSE directly from uvicorn (bypassing Next entirely), plus CORS `allow_origin_regex` to permit the cross-origin fetch.
- **Caltrain geocoding to ZIP centroid:** `kind:"named"` geocoded "Caltrain" to the city area, not a station. Fixed by adding `kind:"transit"` which calls Places `searchNearby` typed by station type (`train_station`, `bus_station`, etc.) with `rankPreference=DISTANCE` per listing, returning the actual nearest station.
- **`exp_base=7` nearly changed:** Briefly considered lowering to 2 — confirmed via git history (commit 05d34cf) this was intentional. `exp_base=2` retries inside Gemini's 60 s RPM window and always fails. Must stay 7. Kept, narrowed `http_status_codes` to `[429]` instead.
- **Two `next dev` instances fighting over `.next` dir:** Couldn't run a second dev server for isolated Playwright testing — lock contention on trace files. Worked around by testing against the user's live :3000 instance.

### What's Left / Next Steps
- **Restart both servers** to pick up the SSE fix: `uvicorn api.server:app --reload --port 8000` + `npm run dev` in `frontend/`. The `.env.local` is in place; just needs a clean restart to load `NEXT_PUBLIC_BACKEND_URL`.
- **Commit Phase 2 changes** — everything is uncommitted (user requested no commits during the session). Suggested commit: `feat(agent+ui): Phase 2 — roommate economics, proximity amenities, transit, RequirementCards, 503 mitigation, SSE fix`.
- **Manager over-cautious clarification** — Manager sometimes asks to confirm state/budget even when clearly stated in a single message. Prompt-tuning issue, not yet addressed.
- **Phase 3 (P3-1/P3-2):** action buttons (schedule tour, save apartment). Phase 4: Mapbox + polish. See plan file at `~/.claude/plans/i-want-to-extend-enhance-tidy-pinwheel.md`.
- **P2-2 multi-area search:** parked; `areas` field is in the schema, so future work is additive.
- **P2-4 route-level transit (Tier 2+):** parked as P2-4-FU; Transitland GTFS preferred if implemented.

### Session Stats
- Commits made: 0 (user requested no commits)
- Files changed: 17 (914 insertions, 78 deletions in working tree)
- New files: `frontend/components/chat/RequirementCards.tsx`, `frontend/app/api/chat/[sessionId]/route.ts`, `frontend/.env.local`, `data/places_usage.json`

---

## Session: 2026-06-23
**Start:** approx. 00:00 | **End:** 01:01 | **Duration:** ~1 hr

### What We Built / Changed
Implemented **Phase 1 + Foundation** of the ApartmentFinder extensions backlog — done *forward-compatibly* on purpose, since a prior attempt had been discarded for baking Phase-1 assumptions that clashed with Phase 2. First reconciled a docs-vs-code discrepancy (CLAUDE.md/memory claimed Phase 1 shipped, but the code still had the CSV fallback and old geocoding). Then landed the Foundation contract (expanded requirements + listing schema, deterministic structured-intake path) and the Phase-1 accuracy fixes: dropped the local CSV, fixed landmark resolution via Google Places (New), US-only validation, 5 results, and price-uncapped budget-overflow intelligence. After a manual test pass, fixed three more issues the user surfaced: a Summarizer-vs-cards ranking mismatch (made the Summarizer the ranking authority and had the UI reorder to match via a hidden marker), a RentCast per-run counter that exhausted the provider server-wide, and a long-broken Apify integration (wrong actor input schema → always 400). Everything verified with offline unit tests + targeted live API checks.

### Changes Made
Full detail in [`changelogExplanation.md`](changelogExplanation.md) under the `[2026-06-23 01:00]` entry. High-level:
- **Backend** (`tools.py` heavily, `instructions.py`, `agent.py`, `api/server.py`, `api/session_manager.py`, `main.py`): CSV removed; Places-based landmark fix; US-state validation; F1/F2 schema; budget-overflow partition; F3 structured-intake via event `state_delta`; Summarizer ranking marker + `output_key`; RentCast per-run reset; full Apify schema rewrite.
- **Frontend** (`types.ts`, `parseApartments.ts`, `useChat.ts`, `api.ts`, `ApartmentCard.tsx`, `MapView.tsx`): listing fields end-to-end, ranking reorder + marker hiding, over-budget badge, 5-rank arrays.
- **Docs/tests**: CLAUDE.md aligned to reality; `test_tools.py` offline + opt-in live suite.

### Problems Encountered & How They Were Resolved
- Docs/memory claimed Phase 1 was shipped but code disagreed → verified against actual code and implemented against reality; corrected the memory note.
- Places API returned 400 → `locationRestriction` only accepts a rectangle, not a circle; switched to `locationBias` circle (which the plan specified anyway).
- F3 state seeding didn't persist → in-memory ADK `get_session` returns a copy; rewrote to persist via `append_event(state_delta=…)`.
- Ranking mismatch (chat ≠ cards) → made the Summarizer the ranking authority emitting a hidden `<!--RANKING:[ids]-->` marker; frontend reorders cards/pins to match.
- "No apartments" after 2 searches → RentCast per-run counter was process-global (reset per call now) **and** Apify was 400'ing (wrong actor schema → rebuilt with `searchUrls`+`searchQueryState`+`MAP_MARKERS`).

### What's Left / Next Steps
- **Checkpoint reached** — Phase 1 + Foundation complete, nothing committed (changes in working tree for review).
- Backlog (`~/.claude/plans/i-want-to-extend-enhance-tidy-pinwheel.md`): **FU-1** (re-run pipeline when the user changes requirements after results) still open; **Phase 2** (bed/bath UI, multi-area, roommate economics, proximity, RequirementCards) and **Phases 3–4** deferred. Mapbox token to be added when P4 starts.
- Minor deferred notes: layout filter is min-semantics (toggle documented); Apify building markers render as `0bd/0ba`.

### Session Stats
- Commits made: 0 (working tree only)
- Files changed: 15 (+~858 / −224)

---

## Session: 2026-06-04 (evening)
**Start:** approx. 20:00 | **End:** 22:15 | **Duration:** ~2 hrs 15 mins

### What We Built / Changed
This was a large UX overhaul session. The goal was to redesign the app to feel like a polished product — landing page, smooth phase transitions, ChatGPT-style sidebar — while also fixing a long-standing bug where commute times never appeared on apartment cards. We did a full root-cause analysis on the commute bug (ANALYST_PROMPT wrote human-readable text but the parser expected raw Google Maps JSON), fixed it with both a prompt update and a more resilient parser. On the UX side: the app now has a three-phase layout (`landing` → `chatting` → `results`) with CSS transitions, a persistent dark sidebar showing dynamic session titles like "Apartments in Austin, TX < $2,000", a centered Claude-style landing input, a Stop button that cancels both the browser stream and the backend pipeline, and a significantly richer map (amber landmark pin, road-following route polylines fetched on demand, popups with photo/rank/commute). Midway through, hitting the run exposed a second bug: adding `"photos": []` to the apartment JSON broke the non-greedy regex parser (it stopped at the first `]` inside `photos: []`), causing the results panel to never appear. That was fixed with a proper bracket-counting parser.

### Changes Made
Full detail in [`changelogExplanation.md`](changelogExplanation.md) under the `[2026-06-04 22:15]` entry. High-level:
- **Backend** (4 files): landmark geocoding in `store_requirements`; photos from Apify; cancel flag support in session_manager; `/cancel` + `/api/directions` endpoints in server.py; `polyline` added to requirements.
- **Frontend hooks/types** (3 files): `useChat.ts` — AbortController stop, landmark state, session title from user_requirements; `types.ts` — `LandmarkInfo`, `photos?`; `api.ts` — external abort signal forwarding.
- **Frontend components** (5 files): `AppShell.tsx` — phase machine + persistent sidebar; `ChatPanel.tsx` — landing UI + Stop button; `ConversationSidebar.tsx` — drawer → persistent dark panel; `MapView.tsx` — landmark pin, route polyline, richer popup; `ResultsPanel.tsx` — landmark prop, default to map tab.
- **Parser** (`parseApartments.ts`): replaced non-greedy regex with bracket-counting parser; added text-format commute fallback.
- **Agent prompt** (`instructions.py`): ANALYST_PROMPT updated to embed raw commute JSON and carry photos.

### Problems Encountered & How They Were Resolved
- **Commute times never showing** → Root cause: ANALYST_PROMPT told the agent to write "25 min commute (15 miles)" but `parseApartments.ts` searched for `"rows": [...]` raw JSON. Fixed by updating the prompt AND adding a text-format fallback in the parser.
- **Results panel/map tab disappeared after changes** → Root cause: adding `"photos": []` to apartment JSON created a nested `]` inside the array. The old non-greedy regex `\[[\s\S]*?\]` stopped at the first `]` found (inside `photos: []`) and never captured the full apartment array — so `apartments` was always empty and the phase never transitioned to `"results"`. Fixed with a bracket-counting parser that handles arbitrary nesting.
- **AbortController disconnected from `chatStream`** → `chatStream` created its own internal `AbortController` with no external hook. Fixed by adding an optional `signal` parameter that forwards to the internal controller via an `abort` event listener.

### What's Left / Next Steps
- **Test commute fix end-to-end** — Run a real search and verify CommuteBadge shows duration + distance. Check browser DevTools Network tab to confirm `analyst_dossier` in the SSE state event contains a `"rows"` JSON block.
- **Test Stop button** — Trigger a search and click Stop mid-run; confirm SSE stream closes, input re-enables, and server logs show the pipeline terminated.
- **Verify landmark pin** — After a search completes, switch to Map tab and confirm an amber star marker appears at the commute destination.
- **Verify route polyline** — Click an apartment pin and confirm a road-following line appears between it and the landmark.
- **Photos are Apify-only** — RentCast doesn't return photos. Photos in popups will only appear if the Apify provider is active (not local CSV fallback).
- **"Powered by Gemini 2.5 Flash" subtitle** in `ChatPanel.tsx` (landing UI) is cosmetically stale — 3 of 4 agents now use OpenAI. Minor update if branding matters.
- **Session titles need a live search to appear** — Sessions created before this session won't have dynamic titles (they show the first 60 chars of the first message). Only new searches get the "Apartments in X, Y < $Z" format.

### Session Stats
- Commits made: 0 (all changes uncommitted)
- Files changed: 16
- Net lines: +810 / -300

---

## Session: 2026-06-04
**Start:** approx. 17:30 PDT | **End:** 18:32 PDT | **Duration:** ~1 hr (continuation of prior context)

### What We Built / Changed
This session was a continuation of a prior context that had hit its limit mid-way through fixing several bugs. The work carried over included: splitting the agent models so OpenAI (gpt-4o-mini via LiteLlm) handles 3 of 4 agents and Gemini is kept only for the Reviewer (which needs native `google_search` grounding), cutting Gemini API load by ~75%. The provider chain was fixed so empty RentCast results fall through to Apify/CSV instead of short-circuiting. The Map toggle crash was diagnosed — `react-leaflet@5` requires React 19 but the project uses React 18, causing an internal context API mismatch. Downgrading to v4.2.1 resolved it. Several other UI fixes were bundled into the same commit: separate message bubbles per agent author (Manager and Summarizer no longer merge), apartment cards persisted to localStorage for past-session restore, and Analyst/Reviewer pipeline status detection via tool-call names as a fallback when ADK's `event.author` doesn't carry sub-agent names.

### Changes Made
No new `changelogExplanation.md` entry exists for this session. All changes landed in commit `c5f4fda`:
- `apartment_finder/agent.py` — model split: `gemini_model` (Reviewer only) + `openai_model` (all others)
- `requirements.txt` — added `litellm`
- `apartment_finder/tools.py` — provider chain continues on empty results instead of early-returning
- `api/session_manager.py` — tool-call-based Analyst/Reviewer status detection added as fallback
- `frontend/components/results/ResultsPanel.tsx` — `next/dynamic` named export wrapped as `{ default: m.MapView }`
- `frontend/hooks/useChat.ts` — per-author bubble splitting + apartment localStorage persistence + restore
- `frontend/package.json` + `package-lock.json` — react-leaflet 5→4.2.1

### Problems Encountered & How They Were Resolved
- **"render is not a function" on Map toggle** → `react-leaflet@5` requires React 19; project uses React 18. The internal Context.Consumer API differs between versions, producing `updateContextConsumer` crash. Downgraded to `react-leaflet@4.2.1` which is React 18-compatible.
- **npm install peer dep conflict** → Pre-existing eslint version conflict. Resolved with `--legacy-peer-deps`.
- **gemini-2.0-flash proposed as rate-limit solution** (prior context) → Model was shut down June 1, 2026. Switched plan to `gemini-2.5-flash-lite` for Reviewer and OpenAI for all other agents.

### What's Left / Next Steps
- **"Powered by Gemini 2.5 Flash" subtitle** in `frontend/components/chat/ChatPanel.tsx` (~line 67) is now stale — 3 of 4 agents use OpenAI. Minor cosmetic update needed.
- **Status bar fix unconfirmed** — the tool-call-based Analyst/Reviewer detection was just implemented; hasn't been verified live yet by the user.
- **GCP deployment** still pending (Cloud Run + Vercel, swap to `VertexAISessionService`).
- **Past sessions before this commit** won't restore result cards (no `apt_apartments_*` in localStorage); only new sessions will.

### Session Stats
- Commits made: 1 (`c5f4fda`)
- Files changed: 8
- Lines changed: +95 / -29

---

## Session: 2026-06-01
**Start:** approx. 14:00 | **End:** 16:18 | **Duration:** ~2 hrs 15 mins

### What We Built / Changed
The goal was to give ApartmentFinder AI a real, browser-accessible UI. The whole session was spent designing and building a production-grade React app on top of the existing Python ADK agent — no changes to any agent code, just a new layer on top. We ended up with a Next.js 14 frontend (split chat + results panel with a Leaflet map) backed by a FastAPI server that wraps the ADK `InMemoryRunner` and streams Server-Sent Events to the browser. After getting the core working, two bugs were caught and fixed during live testing: a Windows IPv6/IPv4 proxy mismatch that silently blocked all API calls, and an unhandled 429 rate-limit exception that was crashing the entire ASGI process. The agent pipeline status indicator was also redesigned from a buried inline pill to a permanent 4-step progress tracker between the chat and the input bar.

### Changes Made
All changes from this session are in `changelogExplanation.md` under the **[2026-06-01 16:16]** entry. That entry covers all 18 new/modified files with plain-English explanations of each decision.

Key files created:
- `api/server.py` + `api/session_manager.py` — FastAPI SSE backend
- `frontend/components/` — AppShell, ChatPanel, AgentStatus, MessageBubble, ResultsPanel, ApartmentCard, CommuteBadge, MapView, ConversationSidebar
- `frontend/hooks/useChat.ts` — SSE stream parser + session lifecycle
- `frontend/lib/` — typed API wrappers, shared types, analyst dossier parser

### Problems Encountered & How They Were Resolved
- **`ECONNREFUSED ::1:8000` on first API call** → Windows resolves `localhost` to IPv6 `::1` but uvicorn binds to IPv4. Fixed by hardcoding `127.0.0.1` in both `next.config.mjs` (proxy destination) and the uvicorn startup command in `package.json`.
- **Next.js 16 install failed** → Node v18 is installed; Next.js 16 + `@tailwindcss/oxide` require Node ≥ 20. Downgraded to Next.js 14 + Tailwind CSS v3, and replaced all Tailwind v4 syntax in the generated scaffold files.
- **`next.config.ts` rejected at build** → Next.js 14 doesn't support `.ts` config files. Renamed to `next.config.mjs` and removed the TypeScript type annotation.
- **429 rate-limit crash (ASGI process died)** → The `ClientError: 429` from Gemini propagated up through the async generator chain uncaught, killing the server. Wrapped `runner.run_async()` in `try/except` in `session_manager.py`; on 429 it now yields a friendly `error` SSE event with the retry delay, then closes cleanly.
- **Agent status never visible** → The pipeline status pill was rendered inside the scrollable messages div — it appeared below all messages and scrolled out of view. Moved to a fixed slot between the message list and the input bar, and redesigned as a 4-step pipeline tracker with checkmarks for completed steps.

### What's Left / Next Steps
- **Session restore from sidebar**: Clicking a past session in the history sidebar currently resets to a blank chat rather than replaying the conversation. Full restore would require storing message history server-side (or in localStorage) and re-hydrating both the chat and the results panel.
- **Apartment card population reliability**: The `parseAnalystDossier()` parser in `frontend/lib/parseApartments.ts` uses regex to find JSON arrays embedded in the Analyst's prose output. If the Analyst's format changes, cards may not populate. A more robust approach would be a dedicated structured output endpoint.
- **GCP deployment**: The plan for production is FastAPI on Cloud Run + Next.js on Vercel, with a one-line swap from `InMemorySessionService` → `VertexAISessionService` in `session_manager.py` for Firestore-backed session persistence. Not done yet.
- **Gemini free-tier quota**: 5 RPM is very low for testing the full 4-agent pipeline (Manager + Analyst + Reviewer + Summarizer = 4+ model calls per search). Consider upgrading to a paid tier or adding a retry-with-backoff UX (show countdown timer when rate-limited).
- **`.gitignore` update**: `frontend/node_modules/` and `frontend/.next/` should be added to `.gitignore` before the first commit of the frontend code.

### Session Stats
- Commits made: 0 (all changes uncommitted)
- New source files created: 20 (in `api/` and `frontend/`)
- Existing files modified: 4 (`requirements.txt`, `package.json`, `.gitignore`, `changelogExplanation.md`)
